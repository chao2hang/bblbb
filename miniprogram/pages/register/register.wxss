.reg-head {
  padding-top: 32rpx;
}

.reg-login {
  text-align: center;
  margin-top: 28rpx;
  color: var(--accent);
}

.reg-error {
  margin-top: 24rpx;
  padding: 20rpx 24rpx;
  background: var(--accent-soft);
  color: var(--accent);
  border-radius: 12rpx;
  font-size: 26rpx;
}

.ph {
  color: var(--ink-3);
}
