//! M07-LEVELS-03..09：签到与活动奖励域。
//!
//! - [`checkin`]：签到事件判定（有效业务页面/排除静态/预取/爬虫/健康检查）、
//!   用户时区日界线（`users.timezone` → 站点时区回退，固定偏移解析）与连续天数。
//! - [`service`]：领取引擎（`activity_claims` 唯一去重 + 账本幂等）、每日上限、
//!   延迟确认/撤销（反向补偿流水）、反刷（自赞/撤赞重赞/被处罚用户/限流）与
//!   管理配置/任务（reason + 审计 + 版本化）。

pub mod checkin;
pub mod service;
