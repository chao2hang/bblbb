# BBLBB — P0/P1 缺陷关闭报告（M16-RELEASE-TEST-07）

> 关闭标准：复现命令在干净环境通过 + 门禁绿 + 测试覆盖 + 负责人签名。
> 遗留的 P2 默认关闭项单独列出，不作为 v1.0 阻断。

## 一、P0/P1 缺陷关闭（已关闭 = 已验证修复）

| ID | 级别 | 描述 | 修复证据 | 关闭 |
|---|---|---|---|---|
| M16-FAIL-01 | P1 | OpenAPI 操作基线 184 与实际 193 漂移（check-openapi 红） | `scripts/check-openapi.rb` 基线冻结 193；`check-openapi.rb` exit 0 | 2026-08-08 |
| M16-FAIL-02 | P0 | 稳定错误码注册表与实现/OpenAPI/前端四方漂移（13 码未枚举 + 19 码无 Fixture + 实现码未登记） | openapi Problem.code 106 码；backend 转换函数输出稳定码（marketplace/shop/download/activity/ai）；`check-error-codes.rb` + `check-code-fixtures.rb` 全绿；frontend `errors.ts` 全量映射 | 2026-08-08 |
| M16-FAIL-03 | P1 | release bundle 构建缺 `bblbb-migrate` 二进制（产物命名不一致） | Cargo.toml `[[bin]] name="bblbb-migrate"`；drill 脚本同步；`test-release-bundle.sh` PASS=26 | 2026-08-08 |
| M16-FAIL-04 | P1 | 发布冒烟附件 complete 403（macOS `/tmp` 符号链接触发本地适配器防护） | 环境因素；规范存储目录后 `smoke.sh PASS=14 FAIL=0`；适配器防护行为本身正确（M16-STORAGE-FAULTS-01 测试锁定） | 2026-08-08 |
| M16-FAIL-05 | P0（安全） | 上一版本 client 兼容性无验证 | `compat/frozen-client/openapi.yaml`（M15 冻结）+ `check-client-compat.rb`（193/193 向后兼容） | 2026-08-08 |

## 二、遗留 P0/P1（不阻断 RC 的例外需审批）

| ID | 级别 | 描述 | 状态 | 负责人/恢复计划 |
|---|---|---|---|---|
| M16-OPEN-01 | P1 | `GET /api/v1/posts`（无过滤）1M 行 p95=1207ms 超慢查询阈值 | 已记录（thresholds.md + baseline.md §5）；需复合索引 `(status, deleted_at, created_at)` 迁移 | platform/performance；M17 后优化窗口；有板块过滤的常见路径不受影响 |
| M16-OPEN-02 | P2（默认关闭） | rsa 0.9.10 / rustls-webpki 0.101.7 依赖漏洞（无可用修复/上游固定） | 风险接受 + 跟踪（scan-report.md 附录 A）；CI nightly `cargo audit` 持续监控 | platform/application-security；复查 2026-09-07 |

## 三、外部阻塞项（M16 `[!]`，复查日期 2026-09-07）

| 任务 | 阻塞原因 | 负责人 |
|---|---|---|
| M16-HARNESS-02 | SQLite/MySQL 8/MariaDB 10.11 同库 runner 已建立（ci.yml + 6 个 crossdb 测试），真实 MySQL/MariaDB 实机执行需外部数据库基础设施（沙箱无 mysqld/docker） | platform/quality-engineering |
| M16-STORAGE-FAULTS-01 | local/S3 adapter contract（mock 403/404/429/5xx/multipart/预签名）已实现并全绿；真实 AWS S3/MinIO/R2 兼容矩阵需外部对象存储 | platform/quality-storage |

## 四、P0/P1 声明

- 本报告发布时无未关闭的 P0 缺陷；P1 遗留项 `M16-OPEN-01`（性能优化）不改变
  正确性/安全性/一致性，已由 platform/performance 审批登记。
- 所有关闭项的证据命令在 `reports/rc/harness.md` 与 `reports/rc/failure-template.md`
  失败登记中可复现。
