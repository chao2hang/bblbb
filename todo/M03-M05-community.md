# M3-M5：用户、权限、内容与社区治理

> 总索引：[`../TODO.md`](../TODO.md)
> 通用规则和证据格式见 [`M00-M02-foundation.md`](M00-M02-foundation.md)。
> 本文件所有 checkbox 都是唯一叶子任务；工作包标题和出口门槛不重复计数。

---

<a id="m3"></a>

# M3：用户资料、授权、板块与标签

**完成定义：** 用户公开/私有投影严格分离；角色和对象授权只有 Rust 裁决；板块、标签和搜索仓储在三数据库行为一致。

## M03-SCHEMA：用户、角色与板块数据模型

**元数据：** `P0` · `owner=backend-users` · `risk=high` · `depends=M02-UX` · `blocked=none`
**目标文件：** `migrations/{sqlite,mysql,mariadb}/`、`backend/src/users/`、`backend/src/authz/`、`backend/src/boards/`
**验收：** 三数据库空库/升级迁移、约束和 repository contract 通过。

- [x] `M03-SCHEMA-01` `P0` `[45m]` 新增用户资料、隐私设置、展示偏好、等级缓存和 profile revision 字段迁移。证据：files=migrations/{sqlite,mysql,mariadb}/0019_profile.sql,backend/tests/profile_schema.rs,docs/SCHEMA.md；commands=cargo fmt --check 通过; cargo clippy --all-features --all-targets 0 警告; cargo test --all-features 全绿（含 profile_schema 5 项 + migration_equivalence 4 项）; make check（迁移等价 + lifecycle + Roadmap 783 叶子 OK、OpenAPI 183/183）; contract=users 新列：level（默认 1，可重建等级缓存）、level_updated_at（NULL=未计算）、avatar_attachment_id（软引用附件）、signature、last_login_at、delete_requested_at、deleted_at；user_preferences（timezone 默认 UTC/locale 默认 zh-CN/theme_name/notification_json，行首访惰性创建，FK 级联）；user_privacy（email_visible_to 默认 nobody、profile_visible_to 默认 everyone，CHECK 约束三库强制）；profile_revisions（UNIQUE(user_id,revision)、changes_json、actor_user_id，FK 级联，资料写操作同事务写 revision）; commit=bb385cb; review=三库迁移等价 + 列契约/默认值/CHECK/唯一/级联 5 项测试 + make check 全绿
- [x] `M03-SCHEMA-02` `P0` `[30m]` 新增头像与 Cover 的稳定 attachment_id 引用，禁止保存远程 URL 或签名 URL。证据：files=migrations/{sqlite,mysql,mariadb}/0020_attachment_refs.sql,backend/tests/profile_schema.rs,docs/SCHEMA.md；commands=cargo fmt --check 通过; cargo clippy --all-features --all-targets 0 警告; cargo test --all-features 全绿（profile_schema 6 项 + migration_equivalence 4 项 + migration_lifecycle 8 项）; make check（迁移等价/lifecycle + Roadmap 783 叶子 OK、OpenAPI 183/183）; contract=users 新列 cover_attachment_id（TEXT NULL 软引用）；头像/Cover 引用约定：仅存附件 UUID（ProfileCoverSet.attachment_id format: uuid），禁止远程 URL/签名 URL——来源与格式校验在 M3-PROFILE 服务层，attachments 表（M6 存储）落地后补外键；测试验证两列存在、UUID 往返可读、未设置默认 NULL; commit=5dfcf8b; review=三库迁移等价 + profile_schema 6 项 + make check 全绿
- [x] `M03-SCHEMA-03` `P0` `[45m]` 新增 roles、permissions、role_permissions 和全局 user_role_assignments 迁移。证据：files=migrations/{sqlite,mysql,mariadb}/0021_rbac.sql,backend/tests/rbac_schema.rs；commands=cargo fmt --check 通过; cargo clippy --all-features --all-targets 0 警告; cargo test --all-features 全绿（rbac_schema 5 项 + migration_equivalence 4 项）; make check（迁移等价/lifecycle + Roadmap 783 叶子 OK、OpenAPI 183/183）; contract=roles（name 唯一、is_system 内置角色、不存权限 JSON 避免双事实来源）；permissions（name 唯一对应 x-permission、risk_level normal/sensitive/system 默认 normal + CHECK 三库强制、is_system）；role_permissions（复合主键 (role_id,permission_id)，删角色/权限级联）；user_roles（复合主键 (user_id,role_id) 全局 assignment，granted_by/granted_at/expires_at 可空永久，删用户级联；板块级见 SCHEMA-04 board_role_assignments）；SCHEMA.md §5 落地; commit=f7e1004; review=三库迁移等价 + rbac_schema 5 项 + make check 全绿
- [x] `M03-SCHEMA-04` `P0` `[45m]` 新增 boards、board_roles 和带有效期的 board_role_assignments 迁移。证据：files=migrations/{sqlite,mysql,mariadb}/0022_board_roles.sql,backend/tests/board_schema.rs,docs/SCHEMA.md；commands=cargo fmt --check 通过; cargo clippy --all-features --all-targets 0 警告; cargo test --all-features 全绿（board_schema 3 项 + migration_equivalence 4 项）; make check（迁移等价/lifecycle + Roadmap 783 叶子 OK、OpenAPI 183/183）; contract=boards 新列：parent_id（软自引用层级，ALTER 不能带 FK，层级/环路校验在服务层）、visibility（public/members/restricted/hidden 默认 public + CHECK）、posting_mode（normal/approval/readonly/closed 默认 normal + CHECK）；board_roles（复合主键 (board_id,role_id)——板块启用哪些角色，删板块/角色级联）；board_role_assignments（id PK + UNIQUE(board_id,user_id,role_id)、granted_by/granted_at/expires_at 可空=永久，删板块/用户/角色级联）；SCHEMA.md §6 同步（board_roles 落地，父节点软引用说明）; commit=50006ba; review=三库迁移等价 + board_schema 3 项（新列/默认值/CHECK/复合主键/UNIQUE/级联）+ make check 全绿
- [x] `M03-SCHEMA-05` `P1` `[30m]` 新增 tags、tag_groups 和 board/tag 关联迁移及 slug 唯一约束。证据：files=migrations/{sqlite,mysql,mariadb}/0023_tags.sql,backend/tests/tags_schema.rs,docs/SCHEMA.md；commands=cargo fmt --check 通过; cargo clippy --all-features --all-targets 0 警告; cargo test --all-features 全绿（tags_schema 4 项 + migration_equivalence 4 项）; make check（迁移等价/lifecycle + Roadmap 783 叶子 OK、OpenAPI 183/183）; contract=tag_groups（slug 全局唯一、sort_order 默认 0）；tags 演进（0003 骨架）：新增 group_id（软引用 tag_groups，ALTER 不能带 FK）、slug（可空，非空时全局唯一，存量行为 NULL，服务层写入时必填）、description 默认 ''、color 可空，usage_count 保留为可重建缓存（listTags 已读取）；board_tags（复合主键 (board_id,tag_id)，删板块/标签级联，与 board_roles 对称）；post_tags 属 M04-SCHEMA-05；SCHEMA.md §6 同步; commit=e14e08c; review=三库迁移等价 + tags_schema 4 项（分组 slug 唯一/标签默认值与 slug 非空唯一/软引用/board_tags 复合主键与级联）+ make check 全绿
- [x] `M03-SCHEMA-06` `P0` `[30m]` 为 role、permission、board 和 assignment 的删除/停用语义设置外键与应用约束。证据：files=migrations/{sqlite,mysql,mariadb}/0024_delete_semantics.sql,backend/tests/delete_semantics.rs,docs/SCHEMA.md；commands=cargo fmt --check 通过; cargo clippy --all-features --all-targets 0 警告; cargo test --all-features 全绿（delete_semantics 5 项 + migration_equivalence 4 项 + migration_lifecycle 8 项，共 56 套件）; make check（迁移等价/lifecycle + Roadmap 783 叶子 OK、OpenAPI 183/183）; contract=boards 软删除（deleted_at 默认 NULL）+ 停用（is_active=0）+ 活跃投影 is_active=1 AND deleted_at IS NULL + 索引 (parent_id,sort_order)/(visibility,deleted_at)；应用约束：roles/permissions 的 is_system=1 不可删除/改名（服务层 M03-AUTHZ 强制；数据库无触发器，测试锁定"数据库不背锅"）、非系统删除级联 role_permissions/user_roles/board_roles/board_role_assignments；boards 存在子板块禁止硬删除（服务层）、删除级联 board_roles/board_role_assignments/board_tags；assignments 的 granted_by 软引用删除置 NULL、expires_at 可空=永久、过期按未生效（M03-AUTHZ-03）；SCHEMA.md §5/§6 同步; commit=97e7a83; review=三库迁移等价 + delete_semantics 5 项（软删/停用投影与索引/删角色级联四表/删权限级联/删板块级联三表/系统行物理可删证明）+ make check 全绿
- [x] `M03-SCHEMA-07` `P0` `[45m]` 编写三数据库 schema 等价 Fixture，覆盖唯一性、过期 assignment 和非法状态。证据：files=backend/tests/schema_fixture.rs,migrations/{sqlite,mysql,mariadb}/0025_board_checks.sql,docs/SCHEMA.md,.github/workflows/ci.yml；commands=cargo fmt --check 通过; cargo clippy --all-features --all-targets 0 警告; cargo test --all-features 全绿（schema_fixture SQLite 流 + migration_equivalence 4 项 + migration_lifecycle 8 项，共 57 套件）; make check（迁移等价/lifecycle + Roadmap 783 叶子 OK、OpenAPI 183/183）; contract=Fixture 覆盖唯一性（tag_groups.slug/tags.slug 非空/board_roles 复合主键/board_role_assignments UNIQUE(board,user,role)/role_permissions/user_roles/post_tags 复合主键）、过期 assignment（expires_at 可空=永久，过期按未生效过滤但保留行）、非法状态（boards.visibility/posting_mode、permissions.risk_level、user_privacy.email_visible_to、posts.status 的 CHECK 三库统一拒绝）、外键完整性（board_tags/role_permissions/board_role_assignments 悬空引用三库统一拒绝）；Fixture 暴露 mysql/mariadb 0022 缺 boards CHECK 的真实缺口，0025 补齐（MySQL 8 3819/MariaDB 4025 分类断言，sqlite 留注释版保持版本平行）；CI mysql-family 任务新增 schema_fixture; commit=51e285c; review=三库迁移等价 + schema_fixture（SQLite 流全过，mysql/mariadb 遵循 auth_crossdb 模式）+ make check 全绿
- [x] `M03-SCHEMA-08` `P1` `[30m]` 同步 `docs/SCHEMA.md`、状态枚举、事件目录和迁移清单。证据：files=docs/SCHEMA.md,docs/STATE-MACHINES.md,docs/EVENT-CATALOG.md；commands=make check（事件目录 OK 22/22、Roadmap 783 叶子 OK、OpenAPI 183/183、TS types 可复现、svelte 0 error）; contract=迁移清单 0001-0025 每版本一行（文件/内容），已发布迁移不可修改、修正新增版本（0025 补齐 0022 缺 CHECK 为例）；§6 boards 列集与 0022/0024 物理列对齐，settings_json/created_by 标注为目标模型由 M03-BOARDS 落地；状态机新增 Board 稳定枚举（visibility/posting_mode/活跃=is_active=1 AND deleted_at IS NULL）与 Authorization 枚举（risk_level/is_system/expires_at 可空永久）；事件目录只收已实现事件（check-event-catalog.rb 强制目录与 events.rs 一致）; commit=ad2010c; review=make check 全绿（事件/路线图/OpenAPI/TS/前端）

## M03-PROFILE：资料投影、隐私与注销匿名化

**元数据：** `P0` · `owner=backend-users` · `risk=high` · `depends=M03-SCHEMA` · `blocked=none`
**目标文件：** `backend/src/users/`、`backend/src/routes/users/`、`backend/tests/users/`、`docs/RETENTION-PRIVACY.md`
**验收：** `Users` tag 的公开、本人和管理投影泄漏测试通过。

- [x] `M03-PROFILE-01` `P0` `[30m]` 定义 public profile、Me 和 admin user 三套显式 DTO，不复用数据库实体序列化。证据：files=backend/src/users/{mod,dto}.rs,backend/src/lib.rs,backend/src/routes/{users,auth}.rs,backend/tests/user_dto.rs,openapi/openapi.yaml,frontend/src/lib/api/{types,client}.ts,frontend/src/lib/api/generated/v1/types.ts,frontend/src/routes/users/[username]/+page.svelte；commands=cargo fmt --check 通过; cargo clippy --all-features --all-targets 0 警告; cargo test --all-features 全绿（user_dto 3 项，共 58 套件）; npm test 185 通过; make check（OpenAPI 183/183、TS types 可复现、svelte-check 0 error、事件目录 22/22、Roadmap 783 叶子 OK）; contract=PublicProfile（id/username/display_name/bio/level/avatar_attachment_id/signature/created_at 严格公开 allowlist，不含邮箱/Session/IP/处罚/审计）；Me（本人投影，Me::from_session 显式构建）；AdminUser（管理投影含 status/删除注销时间，不含凭据）；OpenAPI PublicUser/Me/AdminUser schema 对齐（可空字段 3.1 数组语法），getAdminUser 200 改挂 AdminUser；前端 User 类型 Omit 修复 display_name 可空交集、PublicProfile 接入 getUser; commit=861c411; review=user_dto 3 项（公开键集精确/Me 本人字段/AdminUser 无凭据）+ 全量测试 + make check 全绿
- [x] `M03-PROFILE-02` `P0` `[30m]` 为公开 DTO 建立字段 allowlist，排除邮箱、IP、Session、内部处罚、私有资产和审计信息。证据：files=backend/src/users/dto.rs,backend/tests/user_dto.rs,backend/tests/public_profile_leak.rs,docs/SCHEMA.md；commands=cargo fmt --check 通过; cargo clippy --all-features --all-targets 0 警告; cargo test --all-features 全绿（public_profile_leak 2 项 + user_dto 4 项，共 59 套件）; make check（OpenAPI 183/183、TS types 可复现、svelte-check 0 error、事件目录 22/22、Roadmap 783 叶子 OK）; contract=PUBLIC_PROFILE_ALLOWLIST 精确八字段：id/username/display_name/bio/level/avatar_attachment_id/signature/created_at；公开端点响应键集 ⊆ allowlist 且文本不含敏感值（邮箱/封禁状态/IP/session/password_hash/audit/sanction/delete_requested_at/last_login_at）；deleted 用户 404 且不含邮箱（instance 按 RFC 7807 回显请求路径属请求方自供信息不算泄漏）；SCHEMA.md §4 落地; commit=44bfb57; review=user_dto 键集==allowlist 常量 + 泄漏端到端 2 项 + make check 全绿
- [x] `M03-PROFILE-03` `P1` `[45m]` 实现昵称、简介、签名、时区、主题偏好和隐私设置读取与更新。证据：files=backend/src/users/profile.rs,backend/src/users/{mod,dto}.rs,backend/src/routes/{users,auth}.rs,backend/tests/profile_routes.rs,backend/tests/user_dto.rs,openapi/openapi.yaml,frontend/src/lib/api/generated/v1/{types,enums,index}.ts；commands=cargo fmt --check 通过; cargo clippy --all-features --all-targets 0 警告; cargo test --all-features 全绿（profile_routes 4 项，共 60 套件）; make check（OpenAPI 183/183、TS types 可复现、svelte-check 0 error、事件目录 22/22、Roadmap 783 叶子 OK）; contract=ProfilePatch 扩展 signature/email_visible_to/profile_visible_to（theme 枚举 default/dark/light）；Me 扩展 signature/theme_name/email_visible_to/profile_visible_to；update_profile 单事务：users + user_preferences（INSERT OR IGNORE/INSERT IGNORE 惰性行）+ user_privacy + profile_revisions（COALESCE(MAX)+1，changes_json 字段摘要）；PATCH 只更新出现字段（COALESCE），非法枚举/长度 400 且不写修订，空 PATCH 无副作用；display_name 从 users 表读（会话缓存 PATCH 后会过期）；主题端点 PUT 持久化 + GET 读取; commit=a5ec33e（代码，机械审计基线）+6a45b36（OpenAPI 契约）; review=profile_routes 4 项（全字段持久化/部分更新与惰性行/非法值 400 且无修订/主题持久化）+ 全量测试 + make check 全绿
- [x] `M03-PROFILE-04` `P0` `[30m]` 所有更新使用版本/`If-Match`，并校验长度、Unicode、链接和富文本禁用规则。证据：files=migrations/{sqlite,mysql,mariadb}/0026_users_version.sql,backend/src/error.rs,backend/src/users/profile.rs,backend/src/users/dto.rs,backend/src/routes/{users,auth}.rs,backend/tests/profile_routes.rs,docs/SCHEMA.md；commands=cargo fmt --check 通过; cargo clippy --all-features --all-targets 0 警告; cargo test --all-features 全绿（profile_routes 6 项，共 60 套件）; make check（OpenAPI 183/183、TS types 可复现、svelte-check 0 error、事件目录 22/22、Roadmap 783 叶子 OK）; contract=users.version（0026，默认 1，每次更新 +1）作为 If-Match 来源；update_profile 单事务 UPDATE users SET version=version+1 WHERE id=? AND version=?，rows_affected=0 → 409 version_conflict（AppError 新构造器）；缺失/非法 If-Match → 400；Me 响应含 version；校验规则：Unicode 控制字符（保留 \n\t\r）、角括号（富文本禁用）、危险链接 scheme（javascript:/data:/vbscript:/file:）拒绝，仅 http/https；SCHEMA.md §4 users.version 落地; commit=d10bba0; review=profile_routes 6 项（全字段持久化/部分更新/非法枚举/主题持久化/If-Match 必需+409/文本规则）+ 全量测试 + make check 全绿
- [x] `M03-PROFILE-05` `P1` `[30m]` 实现公开主页和作者资料卡投影，Cover 只返回稳定内容端点引用。证据：files=backend/src/users/dto.rs,backend/src/routes/users.rs,backend/tests/user_dto.rs,backend/tests/public_profile_leak.rs,openapi/openapi.yaml,frontend/src/lib/api/generated/v1/types.ts,docs/SCHEMA.md；commands=cargo fmt --check 通过; cargo clippy --all-features --all-targets 0 警告; cargo test --all-features 全绿（user_dto 6 项 + public_profile_leak 2 项，共 60 套件）; make check（OpenAPI 183/183、TS types 可复现、svelte-check 0 error、事件目录 22/22、Roadmap 783 叶子 OK）; contract=PublicProfile 增加 cover_attachment_id（allowlist 九字段，键集精确断言）；Author DTO（username/display_name/level/profile_url=/users/{username} 稳定端点）；OpenAPI PublicUser 加 cover_attachment_id（可空 3.1 数组语法）、Author display_name 可空 + profile_url required；公开投影头像/Cover 只返回附件 UUID（uuid parse + 无 :// 断言），稳定内容端点 /api/v1/attachments/{id}（M6 落地）；SCHEMA.md §4 约定; commit=f5c475a; review=user_dto 6 项（allowlist 精确/键集==常量/媒体稳定 UUID 非 URL/作者卡稳定 profile_url）+ 泄漏端到端 2 项 + make check 全绿
- [x] `M03-PROFILE-06` `P0` `[30m]` 用户不存在、注销、封禁和 Cover 不可用时返回不泄漏内部原因的安全降级投影。证据：files=backend/src/routes/users.rs,backend/tests/public_profile_leak.rs；commands=cargo fmt --check 通过; cargo clippy --all-features --all-targets 0 警告; cargo test --all-features 全绿（public_profile_leak 3 项，共 60 套件）; make check（OpenAPI 183/183、TS types 可复现、svelte-check 0 error、事件目录 22/22、Roadmap 783 叶子 OK）; contract=查询含 status，Rust 分支：deleted→404 not_found（不泄漏存在性）；banned/pending_delete→200 降级投影（bio/signature/avatar_attachment_id/cover_attachment_id=null，保留 id/username/display_name/level 与全 allowlist 键集，状态字段永不出现）；其余状态正常投影；键集始终 ⊆ PUBLIC_PROFILE_ALLOWLIST（九字段）且不含状态；Cover/头像只回 UUID，不可用由 M6 内容端点负责（投影不 500）; commit=42b5460; review=public_profile_leak 3 项（正常用户无泄漏+媒体稳定 UUID/降级投影置空且无状态泄漏/deleted 404）+ 全量测试 + make check 全绿
- [x] `M03-PROFILE-07` `P0` `[45m]` 实现注销匿名化：保留公开讨论，替换作者标识并断开可识别资料关系。证据：files=backend/src/users/deletion.rs,backend/src/users/mod.rs,backend/tests/account_deletion.rs,docs/SCHEMA.md；commands=cargo fmt --check 通过; cargo clippy --all-features --all-targets 0 警告; cargo test --all-features 全绿（account_deletion 1 项，共 61 套件）; make check（OpenAPI 183/183、TS types 可复现、svelte-check 0 error、事件目录 22/22、Roadmap 783 叶子 OK）; contract=anonymize_user(pool, user_id) 单事务执行器（PROFILE-08 Job 调用）：users 就地匿名化 username_normalized→deleted_user_<id前12位>、email_normalized→<id前12位>@deleted.invalid（保持唯一，RFC 2606 .invalid 不可路由），display_name/bio/signature/头像/Cover/last_login_at/delete_requested_at 清空，status→'deleted'（终止态）、deleted_at 写入、level→1、version+1；DELETE user_preferences/user_privacy（断开可识别资料关系）；全部 Session 立即撤销（revoked_at + revoke_reason='account_deleted'）；公开讨论保留：posts author_id 仍指向匿名化行，公开投影对 deleted 404（PROFILE-06 降级"已注销用户"）；幂等（WHERE status!='deleted'，affected=0 即提交）；profile_revisions 审计保留; commit=cad3b97; review=account_deletion 全契约（匿名化断言不含原名/.invalid 域名/display_name 空/deleted_at 写入/偏好隐私删除/会话全撤销/帖子 author_id 保留/公开 404/幂等/revisions 保留）+ 全量测试 + make check 全绿
- [x] `M03-PROFILE-08` `P0` `[45m]` 实现注销请求、冷却/取消、执行 Job、法律保留例外和不可删除审计。证据：files=migrations/{sqlite,mysql,mariadb}/0027_deletion_lifecycle.sql,backend/src/users/deletion.rs,backend/tests/deletion_lifecycle.rs,docs/SCHEMA.md,docs/STATE-MACHINES.md；commands=cargo fmt --check 通过; cargo clippy --all-features --all-targets 0 警告; cargo test --all-features 全绿（deletion_lifecycle 12 项，共 62 套件）; make check（OpenAPI 183/183、TS types 可复现、svelte-check 0 error、事件目录 22/22、Roadmap 783 叶子 OK、迁移等价 0027 三库通过）; contract=request_deletion（active/restricted→pending_delete，写 delete_requested_at+version+1，单事务 状态变更+入队 account_deletion Job available_at=请求时间+30 天+审计 user.deletion_requested；幂等：已 pending_delete 返回 Ok 保留原时间；去重键 account_deletion:{user_id} 唯一约束保证每用户至多一个有效 Job，取消后再次请求重武装 cancelled/dead 历史 Job）；cancel_deletion（冷却期内→恢复 active/清空 delete_requested_at/排队 Job→cancelled/审计 user.deletion_cancelled；非冷却期→NotPending）；execute_account_deletion（deleted/不存在→AlreadyDeleted；非 pending_delete→NotPending；legal_hold_at 非空→DeferredByLegalHold 跳过+审计 user.deletion_deferred_legal_hold 账户保持 pending_delete；冷却未到→NotYetDue worker 重试；到期→匿名化+审计 user.deletion_executed 同事务提交，anonymize_user_in_tx 抽取）；handle_account_deletion worker 映射（终态/跳过→Succeeded；未到期/DB→Transient；无效 payload→Permanent）；法律保留 0027 legal_hold_at 可空（请求禁止 LegalHold；冷却期内设置→到期跳过，RETENTION-PRIVACY §1 最高优先级）；不可删除审计（audit_logs append-only 无删除 API，注销不删审计）；状态机拒绝 pending(Unverified)/banned(Banned 不绕过 sanction)/deleted 不可再请求取消; commit=53263d2; review=deletion_lifecycle 12 项（请求+入队+审计/重复幂等/取消恢复+Job 取消+审计/未请求取消失败/取消后重请求重武装/未验证+封禁拒绝/已注销拒绝/法律保留禁止请求/法律保留到期跳过+审计+不匿名化/冷却未到延迟/到期匿名化+清理偏好会话+审计保留/worker 管道 claim→handle→complete）+ 全量测试 + make check 全绿
- [x] `M03-PROFILE-09` `P0` `[45m]` 测试 API、SSR、Hover Card、日志和缓存均不泄漏私有用户字段。证据：files=backend/tests/user_leak_sweep.rs,frontend/src/lib/components/UserHoverCard.svelte,frontend/src/lib/testing/privacy-types.ts,frontend/src/lib/testing/ssr/privacy.test.ts,frontend/src/lib/testing/user-page-privacy.test.ts；commands=cargo fmt --check 通过; cargo clippy --all-features --all-targets 0 警告; cargo test --all-features 全绿（user_leak_sweep 2 项，共 63 套件 533 用例）; svelte-check 0 error; vitest 26 文件 187 用例全绿; make check（OpenAPI 183/183、TS types 可复现、svelte-check 0 error、事件目录 22/22、Roadmap 783 叶子 OK）; contract=五面泄漏守卫：API 面（匿名+跨用户访问 /users/{username}、/boards、/tags、/search 无邮箱/密码/password_hash 键；公开主页键集严格== PUBLIC_PROFILE_ALLOWLIST 九字段；本人主页也不含邮箱；未认证 /me→401）；日志面（注册/登录响应与 audit_logs 全部文本列无明文密码，M01-AUDIT-02 脱敏）；SSR 面（UserHoverCard svelte/server 渲染对抗性对象只出公开字段）；Hover Card 面（新增 UserHoverCard.svelte，props 仅 Pick<PublicProfile,'username'|'display_name'|'level'>，链接 /users/{username}；浮层接入由 M3-UI 落地）；缓存面（用户页 onMount getUser 存入 $state 客户端缓存，对抗性响应渲染后 DOM 只含公开字段；hydration/预取缓存 M00-FRONTEND-09 已覆盖）；类型面（privacy-types.ts PublicProfile 编译期断言 无 email/password/password_hash/session_token/totp_secret/reset_token/secret/api_key/status/version，违反即 svelte-check 失败）; commit=efcf170; review=user_leak_sweep 2 项（API 面键集+匿名/跨用户/本人三视角+板块标签搜索+未认证 me 401 / 日志面响应+audit_logs 无明文密码）+ SSR/Hover Card 4 项 + 用户页缓存 1 项 + 类型断言 + 全量门禁全绿
- [x] `M03-PROFILE-10` `P1` `[30m]` 更新 Users operation coverage 的 handler、权限、测试和状态。证据：files=todo/openapi-operation-coverage.json,todo/OPENAPI-COVERAGE.md；commands=ruby scripts/sync-operation-coverage.rb（183/183 重生成，手工字段保留）; ruby scripts/sync-operation-coverage.rb --check 通过; make check（OpenAPI coverage 183/183、Roadmap 783 叶子 OK、事件目录 22/22、TS types 可复现）RC=0; contract=Users 10 个 operation 中 3 个从 baseline_only 提升为 implemented（owner=security-backend，对应 M03-PROFILE 工作包 owner）：getMe（Me DTO allowlist+load_profile_fields 实时 display_name/version+主题持久化；x-permission=authenticated；测试 7 项 http/auth_crossdb/user_dto/profile_routes/account_deletion/deletion_lifecycle/user_leak_sweep）；updateMe（users.version 0026+If-Match→409 version_conflict，长度/Unicode/链接/富文本校验，profile_revisions，主题/隐私/可见性持久化；x-permission=authenticated；测试 3 项）；getPublicUser（PublicProfile 九字段 allowlist 键集断言、deleted→404、banned/pending_delete→降级投影、媒体稳定 UUID；x-permission=user.read_public；测试 4 项）；其余 7 项保持 not_started：admin users CRUD（admin.rs not_implemented 桩，M13-ADMIN）与 profile-cover 三端点（M6 存储依赖）；status 语义 implemented=handler+领域测试完成，跨库/E2E/安全专项由 M16 提升 verified; commit=5811289; review=sync-operation-coverage.rb 重生成后 --check 通过 + make check 全绿
## M03-AUTHZ：RBAC、板块范围与对象授权

**元数据：** `P0` · `owner=security-backend` · `risk=critical` · `depends=M03-SCHEMA,M03-PROFILE` · `blocked=none`
**目标文件：** `backend/src/authz/`、`backend/src/middleware/authz*`、`backend/tests/authz/`、`docs/PERMISSION-MATRIX.md`
**验收：** persona × role × board × object 权限矩阵在 API 层通过；不存在仅前端授权。

- [x] `M03-AUTHZ-01` `P0` `[30m]` 实现 `resource.action` 权限注册表，并拒绝数据库中的未知权限名。证据：files=backend/src/authz/mod.rs,backend/src/lib.rs,backend/tests/authz_registry.rs,docs/PERMISSION-MATRIX.md,docs/SCHEMA.md；commands=cargo fmt --check 通过; cargo clippy --all-features --all-targets 0 警告; cargo test --all-features 全绿（authz_registry 4 项，共 64 套件 540 用例）; make check（OpenAPI 183/183、TS types 可复现、svelte-check 0 error、事件目录 22/22、Roadmap 783 叶子 OK）; contract=backend/src/authz/mod.rs：PERMISSION_REGISTRY v1 全部 68 项权限（PERMISSION-MATRIX §2-8 动作表+附录 operation 级注册表；public/authenticated 为身份标记不入表），每项含 name/risk_level/is_system/description 与 permissions 表列一一对应；RiskLevel normal/sensitive/system（sensitive=§8 高风险重新认证清单 storage/download_billing/marketplace/marketplace.refund_admin/ai/video .manage；system=可改变访问控制本身 role.manage/user.manage/admin.manage，is_system=1 不可删除/改名）；parse_permission_name resource[.sub].action 命名校验（非空、无空段、小写字母数字下划线，允许单段 openid 与三段 ai.task.manage）；verify_db_permissions 读 permissions 表拒绝未注册的未知权限名（UnknownPermissions 列出全部未知），缺失已知权限只报告 missing_from_db（种子由 AUTHZ-02 落地）；registry_is_consistent 自查名称唯一+格式+is_system 与 risk 一致; commit=128bbab（审计基线，含初稿）+dd58ba2（修复 storage.manage 补入、格式放宽兼容 openid/ai.task.manage、lib.rs 接线、集成测试、文档）; review=authz_registry 4 项（空表 Ok+全量缺失/已知放行+计数/未知精确拒绝+修复后恢复/全量注册表 DB 可插入）+ 模块内单测 3 项 + 全量门禁全绿
- [x] `M03-AUTHZ-02` `P0` `[45m]` 实现 administrator、global moderator、board moderator、member 和自定义角色聚合。证据：files=backend/src/authz/roles.rs,backend/src/authz/mod.rs,backend/src/main.rs,backend/tests/authz_roles.rs,docs/PERMISSION-MATRIX.md,docs/SCHEMA.md；commands=cargo fmt --check 通过; cargo clippy --all-features --all-targets 0 警告; cargo test --all-features 全绿（authz_roles 7 项，共 65 套件 550 用例）; make check（OpenAPI 183/183、TS types 可复现、svelte-check 0 error、事件目录 22/22、Roadmap 783 叶子 OK）; contract=BUILTIN_ROLES：member/board_moderator/global_moderator/administrator（is_system=1 不可删除/改名，SCHEMA-06），administrator=All 68 项、其余 List；member 基线（已登录默认角色，无 assignment 生效，§2-7 S/B 动作 46 项，不含 post.moderate/moderation.*/*.manage）；seed_builtin_roles 幂等种子（INSERT OR IGNORE/INSERT IGNORE）写全量权限+4 角色+role_permissions，main.rs 启动自动执行（DB 已连接即种子，失败即启动失败）；aggregate_permissions(user_id,board_id?) = member 基线 ∪ user_roles 未过期全局 ∪ board_role_assignments 未过期板块；过期实时排除；BTreeSet 排序去重+生效角色名；自定义角色与内置同路径; commit=958758c; review=authz_roles 7 项（种子幂等+完整性/无 assignment member 基线/administrator 全量 68/board moderator 板块作用域三视角/global moderator 跨板块/自定义角色聚合+member 基线保留/过期排除未过期保留）+ 模块内单测 3 项 + 全量门禁全绿
- [x] `M03-AUTHZ-03` `P0` `[30m]` 实现带生效/到期时间的全局与板块 assignment 实时判断。证据：files=backend/src/authz/roles.rs,backend/tests/authz_roles.rs,docs/STATE-MACHINES.md,docs/PERMISSION-MATRIX.md；commands=cargo fmt --check 通过; cargo clippy --all-features --all-targets 0 警告; cargo test --all-features 全绿（authz_roles 8 项，共 65 套件 552 用例）; make check（OpenAPI 183/183、TS types 可复现、svelte-check 0 error、事件目录 22/22、Roadmap 783 叶子 OK、迁移等价通过）; contract=assignment_effective_at(granted_at, expires_at, now)：生效当且仅当 granted_at<=now 且 expires_at 为空（永久）或 expires_at>now；边界 granted_at==now 生效、expires_at==now 已到期；未来授权与已到期均按未生效实时判定（STATE-MACHINES §Authorization），过期/未来行保留供审计与恢复（不删除）；aggregate_permissions 的全局（user_roles）与板块（board_role_assignments）查询均加 granted_at<=now 条件（此前仅 expires_at 过滤），未来授权不提前生效；生效窗口=[granted_at, expires_at) 实时判定不依赖缓存; commit=51fae17; review=authz_roles 8 项（新增未来授权不生效+已到期不生效+行保留 2 行）+ roles.rs 单测 7 项（生效窗口 7 断言）+ 全量门禁全绿
- [x] `M03-AUTHZ-04` `P0` `[45m]` 定义动作授权输入：actor、账号状态、角色、board、resource owner、resource state 和 policy version。证据：files=backend/src/authz/decision.rs,backend/src/authz/mod.rs,backend/tests/authz_decision.rs,docs/PERMISSION-MATRIX.md；commands=cargo fmt --check 通过; cargo clippy --all-features --all-targets 0 警告; cargo test --all-features 全绿（authz_decision 2 项，共 66 套件 559 用例）; make check（OpenAPI 183/183、TS types 可复现、svelte-check 0 error、事件目录 22/22、Roadmap 783 叶子 OK）; contract=decision.rs：AccountStatus（users.status 六态，as_str/parse 与 0001 CHECK 一致）；ResourceState（draft/published/hidden/deleted）；BoardVisibility（public/members/restricted/hidden）与 BoardPostingMode（normal/approval/readonly/closed）（0022 CHECK）；ActorContext（user_id+status+已聚合 RoleAggregation AUTHZ-02/03）；ResourceInfo（owner_id+state）；BoardContext（board_id+visibility+posting_mode+deleted）；AuthzInput（actor+permission+board?+resource?+policy_version），AuthzInput::new 拒绝未注册权限名；AUTHZ_POLICY_VERSION="1.0.0"（判定携带，审计/失效检测依据）；Decision（Allow|Deny{reason}）+ DenyReason 八因由（not_authenticated/account_not_allowed/missing_permission/not_resource_owner/resource_state_not_allowed/board_scope_mismatch/policy_version_mismatch/default_deny）；默认拒绝由 AUTHZ-05 落地；is_resource_owner 对象级 owner 判定；账号状态门槛细化由 AUTHZ-06 落地; commit=9ba9555; review=authz_decision 2 项（真实 DB 聚合组装完整输入+owner/deny 语义/未注册权限无法构造输入）+ decision.rs 单测 5 项 + authz 模块单测共 12 项 + 全量门禁全绿
- [x] `M03-AUTHZ-05` `P0` `[45m]` 为 Handler 建立统一 require-action + require-object-scope 调用模式，默认拒绝。证据：files=backend/src/authz/enforce.rs,backend/src/authz/mod.rs,backend/tests/authz_enforce.rs,docs/PERMISSION-MATRIX.md；commands=cargo fmt --check 通过; cargo clippy --all-features --all-targets 0 警告; cargo test --all-features 全绿（authz_enforce 4 项，共 67 套件 569 用例）; make check（OpenAPI 183/183、TS types 可复现、svelte-check 0 error、事件目录 22/22、Roadmap 783 叶子 OK）; contract=enforce.rs：decide_action(roles,permission,status,policy_version) 纯函数（策略版本不符→PolicyVersionMismatch；账号状态门槛仅 Active/Restricted，AUTHZ-06 细化→AccountNotAllowed；permission∈聚合权限→Allow；否则→MissingPermission 默认拒绝，绝无隐式 Allow）；require_action(pool,user_id,status,permission,board_id,policy_version) 加载聚合角色（board_id 携带板块范围）后判定；require_object_scope(actor_id,resource,expected_owner,allowed_states) 对象级 owner+state；denied_reason 审计提取；deny_to_error：NotAuthenticated→401 其余→403；Handler 模式 require_action→拒绝即 deny_to_error→require_object_scope，板块范围经 aggregate(board_id) 实时进入聚合; commit=64c4db7; review=authz_enforce 4 项（完整 handler 模式/默认拒绝三态/deny_to_error 401-403/reason 提取）+ enforce.rs 单测 6 项 + authz 模块单测共 18 项 + 全量门禁全绿
- [x] `M03-AUTHZ-06` `P0` `[30m]` 让未验证、冷静期、restricted、mute、board_mute 和 banned 状态实时参与授权。证据：files=backend/src/authz/enforce.rs,backend/src/authz/decision.rs,backend/tests/authz_enforce.rs,docs/PERMISSION-MATRIX.md；commands=cargo fmt --check 通过; cargo clippy --all-features --all-targets 0 警告; cargo test --all-features 全绿（authz_enforce 6 项，共 67 套件 576 用例）; make check（OpenAPI 183/183、TS types 可复现、svelte-check 0 error、事件目录 22/22、Roadmap 783 叶子 OK）; contract=enforce.rs：AccountGates（status/email_verified/cooldown_until/mute_until/board_mute_until）+ACCOUNT_COOLDOWN_MS=24h；account_gate 默认拒绝：banned/pending_delete/deleted 一律拒（读写不放）；未验证（pending 或 email_verified_at 空）内容写入→EmailUnverified，读/own/验证放行；内容写入权限集（post.create/comment.create/attachment.upload/reaction.create/video.embed）附加门：冷静期未过→InCooldown、restricted→AccountNotAllowed、全局 mute→Muted、本板块 board_mute（携带板块）→BoardMuted；读/own/审核不受 mute/冷静期/restricted 影响；load_account_gates 读 users.status/email_verified_at 推导（mute/board_mute 来自 sanction，M5 落地前 None）；authorize_action=聚合角色→状态门→动作门（Handler 统一入口）；DenyReason 新增 EmailUnverified/InCooldown/Muted/BoardMuted（403）; commit=bc35dc1（AUDIT-20260805-1747 审计基线，含代码）+f1459aa（文档）; review=authz_enforce 6 项（新增 authorize_action 真实 DB 全链路：未验证拒写放读/冷静期拒写/冷静期后放写/banned 全拒；load_account_gates 读行+冷静期推导）+ enforce.rs 单测 11 项（终态全拒/未验证/冷静期+restricted/mute+board_mute/组合门序）+ authz 模块单测共 23 项 + 全量门禁全绿
- [x] `M03-AUTHZ-07` `P0` `[30m]` 规定管理员/版主读取隐藏内容必须显式使用管理投影并记录理由和审计。证据：files=backend/src/authz/hidden.rs,backend/src/authz/mod.rs,backend/tests/authz_hidden.rs,docs/PERMISSION-MATRIX.md,docs/MODERATION.md；commands=cargo fmt --check 通过; cargo clippy --all-features --all-targets 0 警告; cargo test --all-features 全绿（authz_hidden 5 项，共 68 套件 582 用例）; make check（OpenAPI 183/183、TS types 可复现、svelte-check 0 error、事件目录 22/22、Roadmap 783 叶子 OK）; contract=hidden.rs：require_hidden_read(pool,operator,permission,target_type,target_id,reason,request_id) 显式理由（validate_hidden_read_reason 非空/≤200 字符 HIDDEN_READ_REASON_MAX/无控制字符，先于权限判定防存在性探测）→权限（moderation.review|post.moderate 经 authorize_action 默认拒绝）→审计 moderation.read_hidden（actor+target+reason+policy_version+request_id，AuditEntry::moderation_action append-only 不可删除）；HiddenReadError MissingReason/ReasonTooLong/ReasonInvalid/Denied/Database；hidden_read_to_error 理由类→400、Denied→401/403、DB→500；隐藏内容绝不进入普通公开投影/DOM/日志/异常/遥测（SECURITY §6），本路径为 M4 隐藏投影 handler 统一前置; commit=bfff541; review=authz_hidden 5 项（版主+理由放行+审计含 policy_version/无理由拒绝且不写审计/超长拒绝/member 无权限拒绝且不写审计/错误映射）+ hidden.rs 单测 1 项 + 全量门禁全绿
- [x] `M03-AUTHZ-08` `P0` `[45m]` 测试当前板块版主、其他板块版主、全局版主和管理员的正负边界。证据：files=backend/tests/authz_persona.rs；commands=cargo fmt --check 通过; cargo clippy --all-features --all-targets 0 警告; cargo test --all-features 全绿（authz_persona 5 项，共 69 套件 587 用例）; make check（OpenAPI 183/183、TS types 可复现、svelte-check 0 error、事件目录 22/22、Roadmap 783 叶子 OK）; contract=五 persona（管理员/全局版主/当前板块版主/其他板块版主/member）在 authorize_action+聚合上的边界：post.moderate 与 moderation.review——管理员任意板块+无板块全允许；全局版主同样全允许；当前板块版主仅本板块允许（其他板块/无板块拒绝）；其他板块版主仅其板块允许；member 全拒；admin.manage/user.manage/role.manage 仅管理员；member 基线（post.read/reaction.create）全部 persona 允许；板块角色精确聚合（board_roles 仅所属板块）；状态门叠加 banned 板块版主全拒（状态门优先于权限）; commit=81c63ec; review=authz_persona 5 项（moderation 边界/系统权限仅管理员/member 基线全 persona/板块角色仅本板块聚合+banned 叠加）+ 全量门禁全绿
- [x] `M03-AUTHZ-09` `P0` `[45m]` 测试自己/他人、草稿/公开/隐藏/删除、锁定板块和过期 assignment 组合。证据：files=backend/src/authz/decision.rs,backend/tests/authz_object.rs；commands=cargo fmt --check 通过; cargo clippy --all-features --all-targets 0 警告; cargo test --all-features 全绿（authz_object 4 项，共 70 套件 592 用例）; make check（OpenAPI 183/183、TS types 可复现、svelte-check 0 error、事件目录 22/22、Roadmap 783 叶子 OK）; contract=自己/他人×post.edit_own：owner 权限放行+require_object_scope 本人 published 可编辑、他人拒绝（NotResourceOwner）；草稿/公开/隐藏/删除×allowed_states：draft+published 可编辑、hidden/deleted 经普通投影拒绝（ResourceStateNotAllowed），隐藏内容仅 require_hidden_read 显式路径（AUTHZ-07）；锁定板块×post.create：BoardPostingMode::allows_content_write()（normal/approval=true；readonly/closed=false，STATE-MACHINES §Board，M03-BOARDS-03 服务层强制）与权限判定组合；过期 assignment×对象：板块版主到期后失去 post.moderate（即使读取自己隐藏内容），重新授予（同键 UPDATE）恢复; commit=f34b9f4; review=authz_object 4 项（owner 边界/状态边界/锁定板块谓词/过期+对象组合）+ decision.rs 单测 1 项 + 全量门禁全绿
- [x] `M03-AUTHZ-10` `P0` `[30m]` 验证 Feature Flag、前端字段或请求体不能授予任何额外权限。证据：files=backend/tests/authz_no_client_elevation.rs；commands=cargo fmt --check 通过; cargo clippy --all-features --all-targets 0 警告; cargo test --all-features 全绿（authz_no_client_elevation 5 项，共 71 套件 597 用例）; make check（OpenAPI 183/183、TS types 可复现、svelte-check 0 error、事件目录 22/22、Roadmap 783 叶子 OK）; contract=授权判定唯一输入是服务端数据：聚合权限仅来自 DB 角色（member 基线+user_roles+board_role_assignments 含生效窗口 AUTHZ-02/03）、账号状态门仅来自 users 行（AUTHZ-06）；Feature Flag 只控制可选能力路由可用性（feature_gate 关闭即 409 feature_disabled 拒绝，app.rs），核心论坛路由不在 feature_for_path 映射内，Flag 系统只有 set/emergency_off 无授予权限入口；请求体 roles/permissions/policy_version 无任何反序列化路径进入 AuthzInput/aggregate_permissions；Flag 全开/紧急关闭前后授权判定逐项不变，member 仍拿不到 post.moderate/moderation.review/admin.manage/user.manage/role.manage，管理员不受 Flag 影响；恶意请求体 {roles:[administrator],permissions:[admin.manage],...} 可解析但无效，显式 AuthzInput（仅服务端聚合+状态门）与 handler 入口判定逐权限一致；过期/未来管理员授权不生效（生效窗口由服务端 now 判定 AUTHZ-03/09，客户端无法伪造）；核心路由（posts/comments/boards/users/me/attachments 上传/...）不被 Flag 门控，Flag 只可能把能力路由拦成 409 绝无跳过授权的放行通道；能力名与权限注册表完全不相交（结构性）; commit=a69c18a; review=authz_no_client_elevation 5 项（Flag 前后判定不变+全开仍拒管理权限 / 恶意请求体无效+AuthzInput 与 handler 一致 / 生效窗口服务端判定 / 核心路由不门控 / 能力名≠权限名）+ 全量门禁全绿
- [x] `M03-AUTHZ-11` `P0` `[30m]` 自动比较权限注册表、OpenAPI `x-permission` 和权限矩阵文档。证据：files=scripts/check-permission-matrix.rb,docs/PERMISSION-MATRIX.md；commands=cargo fmt --check 通过; cargo clippy --all-features --all-targets 0 警告; cargo test --all-features 全绿（71 套件 597 用例）; make check（Permission matrix OK: 40 distinct x-permission；Registry cross-check OK: 68 items ↔ OpenAPI 38 real ↔ matrix doc 38 appendix rows；OpenAPI 183/183、svelte-check 0 error、事件目录 22/22、Roadmap 783 叶子 OK）; contract=三方自动比较，唯一事实来源 backend/src/authz/mod.rs::PERMISSION_REGISTRY（68 项）：OpenAPI→矩阵文档——每个 x-permission 必须出现在 docs/PERMISSION-MATRIX.md（原 M00-CONTRACT-04 逻辑保留）；OpenAPI→注册表——除身份级标记（public/authenticated）外 x-permission 必须是已注册权限（handler 不能被未注册权限门控）；注册表→矩阵文档——每个注册权限必须被提及（单段权限如 openid 也计）；矩阵文档→注册表——operation 级附录行真实权限必须是已注册权限（防陈旧行）；注册表解析带护栏（PERMISSION_REGISTRY 块缺失或 <60 项直接失败，防格式漂移）；负向验证：临时注入 fake.perm/bogus.perm 均在对应方向报错并给出修复指引后恢复；docs 附录说明同步为三方校验描述（183 个 operation）; commit=b56a54c; review=四向比较全部落地（OpenAPI→doc 保留 / OpenAPI→registry 新增 / registry→doc 新增 / doc→registry 新增）+ 2 组负向注入验证失败路径 + make check 全绿（registry cross-check 68↔38↔38）+ 全量门禁全绿
- [x] `M03-AUTHZ-12` `P1` `[30m]` 更新 Roles/Admin role operation coverage 并附越权测试证据。证据：files=todo/openapi-operation-coverage.json,todo/OPENAPI-COVERAGE.md；commands=ruby scripts/sync-operation-coverage.rb（183/183 重生成，手工字段保留）; ruby scripts/sync-operation-coverage.rb --check 通过; cargo fmt --check 通过; cargo clippy --all-features --all-targets 0 警告; cargo test --all-features 全绿（71 套件 597 用例）; make check（OpenAPI 183/183、route-coverage OK、Permission matrix OK 40 distinct + Registry cross-check 68↔38↔38、svelte-check 0 error、事件目录 22/22、Roadmap 783 叶子 OK）; contract=Roles 工作包 4 个 operation（listAdminRoles/createAdminRole/getAdminRole/updateAdminRole，x-permission=role.manage）从 not_started 提升为 in_progress（owner=security-backend，对应 M03-AUTHZ 工作包 owner），handler 登记 backend/src/routes/admin.rs::*（当前 501 占位，role CRUD 领域逻辑待对应里程碑实现）；越权测试证据 4 文件：authz_persona system_permissions_only_for_administrator（admin.manage/user.manage/role.manage 仅管理员，全局版主/板块版主/member 全拒）+ moderation boundary；authz_no_client_elevation（请求体伪造 roles/permissions 无效、Feature Flag 不能授予权限、过期/未来 assignment 不生效、核心路由只走授权层）；authz_enforce（require-action 默认拒绝+账号状态门）+ authz_roles（聚合 assignment 生效窗口）；覆盖状态分布 baseline_only=3 / implemented=21 / in_progress=4 / not_started=155（183 合计）; commit=69f1841; review=4 个 Roles operation coverage 提升并附越权测试证据 + sync 重生成 --check 可复现（手工字段保留）+ route-coverage/权限矩阵/make check 全绿

## M03-BOARDS：板块、标签与管理 CRUD

**元数据：** `P1` · `owner=backend-content` · `risk=high` · `depends=M03-AUTHZ` · `blocked=none`
**目标文件：** `backend/src/boards/`、`backend/src/tags/`、`backend/src/routes/{boards,tags,admin}/`、`backend/tests/boards/`
**验收：** Boards/Tags/Admin 对应 operation 在三数据库通过权限、分页和版本契约。

- [x] `M03-BOARDS-01` `P1` `[45m]` 实现板块层级读取，限制最大深度并检测循环父级。证据：files=backend/src/boards/mod.rs,backend/src/boards/hierarchy.rs,backend/src/lib.rs,backend/tests/boards_hierarchy.rs；commands=cargo fmt --check 通过; cargo clippy --all-features --all-targets 0 警告; cargo test --all-features 全绿（boards_hierarchy 7 项，共 71 套件 613 用例）; make check（OpenAPI 183/183、write-contract/route-coverage OK、Permission matrix OK + Registry cross-check 68↔38↔38、svelte-check 0 error、事件目录 22/22、Roadmap 783 叶子 OK）; contract=boards.parent_id 软自引用层级（迁移 0022，SCHEMA.md §6）由服务层裁决：MAX_BOARD_DEPTH=4（根=第 1 级，最深 4 级=3 层子板块）；build_hierarchy 迭代式深度计算+环路/自引用/悬空父级检测（栈安全不随链长递归）；环路/自引用/超深=数据完整性故障→读取与写入均硬错误（防无限展开）；悬空父级（父板块软删/停用）读取时提升为根并记录（BoardHierarchy::dangling），写入时拒绝（DanglingParent）；validate_parent 写入侧父级校验（合成树复用 build_hierarchy 判定深度/环路）；load_hierarchy 活动投影（is_active=1 AND deleted_at IS NULL）稳定排序（sort_order, created_at, id）三数据库 Either 加载；有子板块禁止硬删除（has_children/descendant_ids 供服务层裁决，SCHEMA-06 语义）; commit=a63ff40; review=hierarchy.rs 单测 9 项（平铺/嵌套/深度边界 MAX=4 允许 5 超限/自引用/双节点环/三节点环/悬空提升/validate_parent 正负边界）+ boards_hierarchy 7 项（种子 5 根稳定排序（0006 归一化 id）/嵌套 depth+children+BFS 后代/停用与软删移出投影/DB 环路加载失败/DB 超深加载失败/软删父级提升+悬空记录/硬删除裁决）+ 全量门禁全绿
- [x] `M03-BOARDS-02` `P1` `[30m]` 实现板块 slug、标题、说明、排序、状态和发帖规则校验。证据：files=backend/src/boards/validation.rs,backend/src/boards/mod.rs,backend/tests/boards_validation.rs；commands=cargo fmt --check 通过; cargo clippy --all-features --all-targets 0 警告; cargo test --all-features 全绿（boards_validation 4 项，共 72 套件 624 用例）; make check（OpenAPI 183/183、write-contract/route-coverage OK、Permission matrix OK + Registry cross-check 68↔38↔38、svelte-check 0 error、事件目录 22/22、Roadmap 783 叶子 OK）; contract=服务层校验（DB CHECK 只兜底稳定枚举）：slug `[a-z0-9-]+`（小写/数字/连字符，与种子及原型 tech-essay/web-dev 一致）长度 1..=120（与 OpenAPI Slug 参数 minLength:1/maxLength:120 一致）；name（标题）trim 非空/≤100/单行纯文本（无控制字符/换行/角括号）；description ≤2000/纯文本多行/链接仅 http/https（防 javascript:/data:）；sort_order [-100_000,100_000]；is_active 布尔（停用移出活跃投影）；posting_mode normal/approval/readonly/closed（与 0022/0025 CHECK 及 authz BoardPostingMode 一致，readonly/closed 禁发帖由 BOARDS-03 强制）；validate_board_fields（创建全字段）/validate_board_update（部分更新）；slug_exists（三数据库 Either）+boards_slug_uq 唯一索引兜底（同 slug 二次插入 unique violation）；validation_to_error → 400 invalid_request（BOARDS-05 路由层统一映射）; commit=d58c7a4; review=validation.rs 单测 7 项（slug 边界 120/121/大写/空格/中文/连字符；name 空/超长/角括号/换行；description 超长/控制字符/富文本/危险 scheme；sort_order 边界；posting_mode 枚举；创建组合；部分更新）+ boards_validation 4 项（slug_exists 命中种子/新 slug/插入后命中；合法字段插入+readonly；同 slug 撞唯一索引；非法 posting_mode 服务层+DB CHECK 双重拒绝）+ 全量门禁全绿
- [x] `M03-BOARDS-03` `P0` `[30m]` 实现 public/members/restricted/hidden 板块可见性并统一套用授权服务。证据：files=backend/src/boards/visibility.rs,backend/src/boards/mod.rs,backend/src/routes/boards.rs,backend/tests/boards_visibility.rs；commands=cargo fmt --check 通过; cargo clippy --all-features --all-targets 0 警告; cargo test --all-features 全绿（boards_visibility 5 项，共 73 套件 631 用例）; make check（OpenAPI 183/183、write-contract/route-coverage OK、Permission matrix OK + Registry cross-check 68↔38↔38、svelte-check 0 error、事件目录 22/22、Roadmap 783 叶子 OK）; contract=可见性服务门（统一套用 AUTHZ-02/03/05/06）：public 匿名可见；members 有效已登录可见（authorize_action board.read 含账号状态门 banned/pending_delete/deleted 全拒）；restricted 有效已登录+本板块生效角色（aggregate_permissions 板块作用域 board_roles 非空，过期 assignment 不授予）或全局/管理通道（post.moderate 命中管理员/全局版主）；hidden 仅管理权限可读（board.manage/post.moderate/moderation.review 任一）；VisibilityDeny::to_error 匿名→401 authentication_required、restricted 非成员/账号状态→403 forbidden、hidden 无管理权限→404（不泄漏隐藏板块存在性，与 AUTHZ-07 一致）；filter_visible_board_ids 列表投影过滤（hidden 不进入公开列表/搜索，members/restricted 对匿名隐藏），list_boards/get_board 路由接入 AuthSession 匿名=Option + get_board 加 deleted_at 活跃投影; commit=ea4bf67; review=visibility.rs 单测 2 项（deny→401/403/404 状态码；HIDDEN_READ_PERMISSIONS 全部已注册）+ boards_visibility 5 项（public 匿名可见 / members 匿名 401+banned 状态门优先 / restricted 无角色 403+生效角色可见+过期不可见+管理员通道 / hidden 匿名 401+成员 404+全局版主与管理员可见 / 列表过滤四类可见性+管理员见全部）+ 全量门禁全绿
- [x] `M03-BOARDS-04` `P1` `[45m]` 实现板块列表/详情 cursor 分页、稳定排序和 Cache-Control。证据：files=backend/src/boards/pagination.rs,backend/src/boards/mod.rs,backend/src/routes/boards.rs,backend/tests/boards_pagination.rs；commands=cargo fmt --check 通过; cargo clippy --all-features --all-targets 0 警告; cargo test --all-features 全绿（boards_pagination 5 项，共 74 套件 639 用例）; make check（OpenAPI 183/183、write-contract/route-coverage OK、Permission matrix OK + Registry cross-check 68↔38↔38、svelte-check 0 error、事件目录 22/22、Roadmap 783 叶子 OK）; contract=稳定排序 sort_order ASC/created_at ASC/id ASC（id 兜底确定性）；cursor 分页：不透明游标=最后一条已返回项排序键 base64url(no-pad)，服务端仅按 (sort_order,created_at,id) 字典序跳过，不重不漏；非法游标→400；limit 默认 30 范围 1..=100（OpenAPI Limit）；响应对齐 OpenAPI BoardPage {items,page:{next_cursor,has_more}}，Board=ResourceMeta(id/version/created_at/updated_at)+slug/name/description；boards 无独立 version 列以 updated_at（毫秒）为乐观并发版本（BOARDS-05 If-Match 同源）；Cache-Control：匿名公开列表 public max-age=60、已认证列表 private no-store（按请求方裁剪）、公开详情 public、members/restricted/hidden 详情 private；可见性过滤（BOARDS-03）接入列表保持顺序，getBoard 保留可见性门+活跃投影; commit=fbdf54b; review=pagination.rs 单测 3 项（base64url 往返/非法与截断游标拒绝/三元组字典序）+ boards_pagination 5 项（匿名列表稳定排序+ResourceMeta+public cache / limit=2 三页不重不漏+next_cursor 从最后返回键继续 / 非法游标 400 / hidden/members 不进匿名列表+匿名详情 401/404 / 已认证列表 private+公开详情 public+members 401）+ 全量门禁全绿
- [x] `M03-BOARDS-05` `P1` `[45m]` 实现管理员创建/更新板块的版本冲突、reason 和审计。证据：files=backend/src/boards/admin.rs,backend/src/boards/mod.rs,backend/src/routes/admin.rs,backend/src/audit/mod.rs,backend/tests/boards_admin.rs；commands=cargo fmt --check 通过; cargo clippy --all-features --all-targets 0 警告; cargo test --all-features 全绿（boards_admin 7 项，共 75 套件 646 用例）; make check（OpenAPI 183/183、write-contract/route-coverage OK、Permission matrix OK + Registry cross-check 68↔38↔38、svelte-check 0 error、事件目录 22/22、Roadmap 783 叶子 OK）; contract=权限门 authorize_action board.manage（仅管理员，AUTHZ-05 默认拒绝）；create_board 字段校验（BOARDS-02）+可见性枚举（BOARDS-03）+父级校验（BOARDS-01 循环/深度/存在性）+slug 唯一（boards_slug_uq 兜底）→单事务 INSERT+admin.board_create 审计（M01-AUDIT-08 事务内，回滚同步消失）；update_board If-Match 版本冲突（boards.updated_at 为乐观并发版本，BOARDS-04 投影同源；缺头 400、过期 409 version_conflict、UPDATE rows=0 兜底竞态）+部分字段校验+父级/slug 变化校验→单事务 UPDATE+admin.board_update 审计（before/after 白名单字段）；reason 必填（矩阵：所有 admin.* 写操作记录 reason、actor、request_id）；audit 新增 board_change helper + AUDIT_FIELD_ALLOWLIST 补充板块非敏感字段（id/slug/name/description/parent_id/sort_order/posting_mode/is_active/created_at/updated_at）; commit=c21c0f5; review=boards_admin 7 项（创建 200+默认值+审计（reason/effective_role/policy_version）/member 403+缺 reason 400/slug 冲突 409+非法 slug 400/更新正确 If-Match 200+版本递增+before/after 审计/缺 If-Match 400+过期 409/父级循环 400/is_active=false 移出公开列表）+ 全量门禁全绿
- [x] `M03-BOARDS-06` `P1` `[30m]` 实现标签组、标签 slug、展示名和禁用状态读取。证据：files=migrations/{sqlite,mysql,mariadb}/0028_tags_active.sql,backend/src/tags/mod.rs,backend/src/lib.rs,backend/src/routes/boards.rs,backend/src/routes/admin.rs,backend/tests/tags_reading.rs,docs/SCHEMA.md；commands=cargo fmt --check 通过; cargo clippy --all-features --all-targets 0 警告; cargo test --all-features 全绿（tags_reading 3 项，共 76 套件 649 用例，迁移等价 0028 三库通过）; make check（OpenAPI 183/183、write-contract/route-coverage OK、Permission matrix OK + Registry cross-check 68↔38↔38、svelte-check 0 error、事件目录 22/22、Roadmap 783 叶子 OK）; contract=迁移 0028（三库等价）：tags.is_active INTEGER/TINYINT NOT NULL DEFAULT 1（0=禁用→移出公开投影，保留历史与既有关联，与 boards.is_active 语义一致，SCHEMA.md §6）；backend/src/tags/ 服务：load_tag_groups（sort_order 稳定排序）、load_active_tags（is_active=1，usage_count 降序）、load_all_tags（管理端含禁用）；Tag/TagGroup 投影（slug/展示名/description/color/group_id/is_active）；listTags（public）返回启用标签+标签组 groups；listAdminTags（tag.manage 权限门 AUTHZ-05 默认拒绝）返回全部标签含 is_active 禁用状态; commit=3cf83bd; review=tags_reading 3 项（0028 默认 1+禁用移出活跃读取+管理端含禁用+组排序 / listTags 只含启用+组 / listAdminTags 含禁用状态+member 403）+ 迁移等价 0028 三库 + 全量门禁全绿
- [x] `M03-BOARDS-07` `P1` `[45m]` 实现标签创建/更新的唯一性、版本冲突、权限和审计。证据：files=migrations/{sqlite,mysql,mariadb}/0029_tags_version.sql,backend/src/tags/admin.rs,backend/src/tags/mod.rs,backend/src/routes/admin.rs,backend/src/audit/mod.rs,backend/tests/tags_admin.rs,docs/SCHEMA.md；commands=cargo fmt --check 通过; cargo clippy --all-features --all-targets 0 警告; cargo test --all-features 全绿（tags_admin 5 项，共 77 套件 654 用例，迁移等价 0029 三库通过）; make check（OpenAPI 183/183、write-contract/route-coverage OK、Permission matrix OK + Registry cross-check 68↔38↔38、svelte-check 0 error、事件目录 22/22、Roadmap 783 叶子 OK）; contract=迁移 0029（三库等价）：tags.updated_at 默认 0（存量行以 created_at 初始化）作为 If-Match 乐观并发版本（与 boards.updated_at 语义一致）；create_tag name/slug 唯一（tags.name 0003 + tags_slug_uq 0023 兜底）+group_id 存在性（软引用服务层校验）+字段格式→单事务 INSERT+admin.tag_create 审计（M01-AUDIT-08）；update_tag If-Match 版本冲突（缺头 400/过期 409/UPDATE rows=0 兜底竞态）+部分字段校验+admin.tag_update 审计（before/after 白名单字段）；reason 必填（矩阵：所有 admin.* 写操作记录 reason、actor、request_id）；权限门 tag.manage（AUTHZ-05 默认拒绝）；audit 新增 tag_change + allowlist 补 color/group_id/usage_count; commit=b6ae0dc; review=tags_admin 5 项（创建 200+审计 admin.tag_create / member 403+name/slug 冲突 409+非法 group_id 400 / 更新正确 If-Match 200+版本递增+before/after 审计+过期 409 / 改名撞已存在 name 409 / 禁用移出公开 listTags）+ 迁移等价 0029 三库 + 全量门禁全绿
- [x] `M03-BOARDS-08` `P0` `[30m]` 禁止通过板块/标签计数、面包屑或错误差异推断隐藏资源。证据：files=backend/src/boards/visibility.rs,backend/tests/boards_no_leak.rs,backend/tests/boards_visibility.rs,backend/tests/boards_pagination.rs；commands=cargo fmt --check 通过; cargo clippy --all-features --all-targets 0 警告; cargo test --all-features 全绿（boards_no_leak 4 项，共 78 套件 658 用例）; make check（OpenAPI 183/183、write-contract/route-coverage OK、Permission matrix OK + Registry cross-check 68↔38↔38、svelte-check 0 error、事件目录 22/22、Roadmap 783 叶子 OK）; contract=修复错误差异：hidden 板块匿名从 401 改为一律 MissingPermission→404（消除 匿名探测 hidden=401 vs 不存在=404 的差异，与 AUTHZ-07 隐藏内容策略一致）；锁定三面无泄漏：错误差异（hidden 与不存在 slug 匿名/已登录非特权均 404，错误体 code/title/detail/status 完全一致仅 instance/request_id 不同）、计数（公开 Board 投影不含 post_count/visibility/parent_id/posting_mode/is_active/sort_order；禁用标签 usage_count 不进入公开 listTags）、面包屑（公开投影不含 parent_id——隐藏父板块的可见子板块不暴露父级，父板块本身 404）; commit=dcb2f3e; review=boards_no_leak 4 项（hidden 与不存在不可区分（匿名+已登录）/公开投影无计数与面包屑字段/隐藏父板块子板块不暴露/禁用标签计数不泄漏）+ boards_visibility/boards_pagination 断言同步 + 全量门禁全绿
- [x] `M03-BOARDS-09` `P1` `[45m]` 运行父级循环、并发 slug、隐藏板块、跨板块角色和 cursor 不重不漏测试。证据：files=backend/src/boards/admin.rs,backend/src/tags/admin.rs,backend/tests/boards_admin.rs,backend/tests/boards_hierarchy.rs,backend/tests/boards_visibility.rs,backend/tests/boards_no_leak.rs,backend/tests/boards_pagination.rs；commands=cargo fmt --check 通过; cargo clippy --all-features --all-targets 0 警告; cargo test --all-features 全绿（659 用例，78 套件）; make check（OpenAPI 183/183、write-contract/route-coverage OK、Permission matrix OK + Registry cross-check 68↔38↔38、svelte-check 0 error、事件目录 22/22、Roadmap 783 叶子 OK）; contract=五类边界测试：父级循环（boards_hierarchy DB 环路加载失败+路径 / boards_admin update 根移到子板块下 400）；并发 slug（新增 concurrent_create_same_slug_only_one_wins：tokio::join! 两请求同 slug——恰好一个 200、另一个 409（唯一索引兜底），绝不 500/重复行；为支撑竞态 create_board/create_tag INSERT 唯一约束冲突从 500 改 409 write_error helper）；隐藏板块（boards_visibility hidden 门 / boards_no_leak 错误差异与计数/面包屑）；跨板块角色（authz_persona board_roles 仅本板块聚合 / boards_visibility restricted 门生效 vs 过期 vs 管理员通道）；cursor 不重不漏（boards_pagination limit=2 三页合计全部 5 板块）；注：session_login login_ip_rate_limited 满负载并行下偶发超时翻动（1 分钟窗口+Argon2id>60s），隔离稳定通过，非本次引入（前 8 次全量门禁均绿）; commit=612a865; review=并发 slug 真实竞态 + 全量门禁全绿（659 用例 78 套件）+ 五类边界逐项复核
- [x] `M03-BOARDS-10` `P1` `[30m]` 更新 Boards/Tags 对应 operation coverage 和管理端证据。证据：files=todo/openapi-operation-coverage.json,todo/OPENAPI-COVERAGE.md；commands=ruby scripts/sync-operation-coverage.rb（183/183 重生成，手工字段保留）; ruby scripts/sync-operation-coverage.rb --check 通过; cargo fmt --check 通过; cargo clippy --all-features --all-targets 0 警告; cargo test --all-features 全绿（659 用例 78 套件）; make check（OpenAPI 183/183、write-contract/route-coverage OK、Permission matrix OK + Registry cross-check 68↔38↔38、svelte-check 0 error、事件目录 22/22、TS types 可复现、Roadmap 783 叶子 OK）RC=0; contract=M03-BOARDS 工作包 11 个 operation 更新（owner=backend-content，对应工作包 owner）：8 个从 not_started 提升为 implemented——listBoards（cursor 分页+可见性过滤+Cache-Control+公开投影无计数/面包屑，handler boards.rs::list_boards，测试 boards_pagination/boards_visibility/boards_no_leak）、getBoard（board_read_gate 可见性门+活跃投影，测试同左）、listTags（启用标签+标签组，测试 tags_reading）、createAdminBoard（board.manage 门+reason+slug 唯一并发 409+事务审计，测试 boards_admin）、updateAdminBoard（If-Match 版本冲突+父级循环 400+审计，测试 boards_admin）、listAdminTags（tag.manage 门+禁用状态，测试 tags_reading）、createAdminTag（唯一性+group_id+审计，测试 tags_admin）、updateAdminTag（If-Match+审计，测试 tags_admin）；3 个提升为 in_progress（handler admin.rs 501 占位，管理投影领域逻辑待对应里程碑）：listAdminBoards/getAdminBoard/getAdminTag；覆盖状态分布 baseline_only=3 / implemented=29 / in_progress=7 / not_started=144（183 合计）；管理端越权证据：boards_admin/tags_admin（member 403）+ authz_persona（board.manage/tag.manage 仅管理员）+ authz_no_client_elevation（权限不可伪造）; commit=1a26bd3; review=11 个 Boards/Tags operation coverage 更新并附管理端越权证据 + sync 重生成 --check 可复现（手工字段保留）+ 全量门禁全绿（659 用例 78 套件）

## M03-SEARCH-STORE：跨库搜索仓储基础

**元数据：** `P1` · `owner=backend-search` · `risk=high` · `depends=M03-BOARDS` · `blocked=none`
**目标文件：** `migrations/*/`、`backend/src/search/`、`backend/tests/search/`
**验收：** SQLite FTS5、MySQL FULLTEXT、MariaDB FULLTEXT 的索引生命周期和基础查询契约一致。

- [x] `M03-SEARCH-STORE-01` `P1` `[45m]` 定义搜索文档最小模型、公开投影字段、source revision 和 policy revision。证据：files=backend/src/search/mod.rs,backend/src/lib.rs,docs/SEARCH.md,docs/DOCUMENT-STATUS.md；commands=cargo fmt --all -- --check 通过; cargo clippy --all-features --all-targets 0 警告; cargo test --all-features 全绿（search 单测 10 项，共 669 用例 78 套件）; make check（OpenAPI 183/183、write-contract/route-coverage OK、Permission matrix OK + Registry cross-check 68↔38↔38、svelte-check 0 error、事件目录 22/22、TS types 可复现、Roadmap 783 叶子 OK）RC=0; contract=search 模块（docs/SEARCH.md 存储契约 + SearchDocument 实现）：最小模型 11 字段（id/entity_type/title/body/excerpt/slug/author_id/tags/source_revision/policy_revision/indexed_at）；SearchEntityType=post/user/board/tag 与 OpenAPI SearchResult.type 一致；公开投影=SearchResult 五字段（id/type/title/url/excerpt），body/author_id/tags_json/revisions 永不投影；构造即清洗校验——标题 trim 非空 ≤240（OpenAPI maxLength 240）、正文 clean_index_text 清洗（控制字符/连续空白折叠、字符边界截断）非空 ≤100_000、slug `[a-z0-9_-]+` ≤120（下划线兼容 username_normalized）、摘要 excerpt_from_clean 字符边界截断 200 以 … 结尾且绝不包含受限正文；source_revision=源实体 updated_at（与 boards/tags/users If-Match 同源 SCHEMA §6）；policy_revision=max(策略相关行 updated_at)（post.status/post.visibility/board.is_active/board.visibility/作者状态/deleted_at/opt_out），不变式=策略变更必须 bump 对应行 updated_at，非递减单调，陈旧写防覆盖守卫 stored.policy_revision<=candidate（旧 revision 不覆盖新，幂等重建始终生效）；安全文本红线（M03-SEARCH-STORE-05 强制）与三库 FTS 策略（FTS5/FULLTEXT）已写入契约 §6/§7; commit=5e2ae47; review=search 单测 10 项（枚举往返/清洗控制字符与空白折叠/多字节字符边界截断/摘要截断…/revision max 非递减与陈旧快照被拒/构造校验正负边界）+ 全量门禁全绿（669 用例 78 套件）
- [x] `M03-SEARCH-STORE-02` `P1` `[45m]` 创建 SQLite FTS5 迁移、触发/Job 更新策略和重建命令。证据：files=migrations/{sqlite,mysql,mariadb}/0030_search_index.sql,backend/src/search/fts.rs,backend/src/search/mod.rs,backend/tests/search_store.rs,docs/SEARCH.md,docs/SCHEMA.md；commands=cargo fmt --all -- --check 通过; cargo clippy --all-features --all-targets 0 警告; cargo test --all-features 全绿（search_store 3 项 + search 单测 10 项，共 672 用例 79 套件，迁移等价 0030 三库通过）; make check（OpenAPI 183/183、write-contract/route-coverage OK、Permission matrix OK + Registry cross-check 68↔38↔38、svelte-check 0 error、事件目录 22/22、TS types 可复现、Roadmap 783 叶子 OK）RC=0; contract=迁移 0030（三库等价，mysql==mariadb 可执行 SQL 一致）：search_documents 元数据表——rowid（sqlite INTEGER PRIMARY KEY AUTOINCREMENT / mysql BIGINT AUTO_INCREMENT，供 FTS5 external content 映射）、doc_id 全局唯一（search_documents_doc_id_uq）、entity_type（post/user/board/tag，SQLite CHECK）、title/body/excerpt/slug/author_id/tags_json（安全文本长度上限与 SEARCH.md §2/§3/§6 一致）、source_revision/policy_revision/indexed_at；SQLite 另建 search_fts（FTS5 external content，content='search_documents'/content_rowid='rowid'/unicode61）+ 三个同步触发器 search_fts_ai/ad/au——触发器/Job 更新策略：search_documents 由索引 Job 维护（M03-SEARCH-STORE-06），FTS5 由触发器自动同步，Job 不直接写 FTS 表；重建命令统一入口 rebuild_fts（SQLite `INSERT INTO search_fts(search_fts) VALUES('rebuild')` 幂等 / MySQL `OPTIMIZE TABLE search_documents`）；MySQL/MariaDB FULLTEXT 在 0031/0032 加入，InnoDB 原生随行更新无需触发器; commit=7cbf038; review=search_store 3 项（迁移建立 search_documents+search_fts external content+3 触发器 / insert-update-delete 自动同步（MATCH 旧 token 不再命中） / rebuild 两次幂等不丢不重+重建后触发器仍同步）+ 迁移等价 0030 三库 + 全量门禁全绿（672 用例 79 套件）
- [x] `M03-SEARCH-STORE-03` `P1` `[45m]` 创建 MySQL 8 FULLTEXT 迁移、分词限制和重建命令。证据：files=migrations/{sqlite,mysql,mariadb}/0031_search_fts.sql,backend/tests/search_store.rs,docs/SEARCH.md,docs/SCHEMA.md,.github/workflows/ci.yml；commands=cargo fmt --all -- --check 通过; cargo clippy --all-features --all-targets 0 警告; cargo test --all-features 全绿（672 用例 79 套件；session_login login_ip_rate_limited 满负载偶发翻动与 BOARDS-09 同源，隔离 4.63s 通过，重跑全绿）; make check（OpenAPI 183/183、write-contract/route-coverage OK、Permission matrix OK + Registry cross-check 68↔38↔38、svelte-check 0 error、事件目录 22/22、TS types 可复现、迁移等价 0031 三库、Roadmap 783 叶子 OK）RC=0; contract=迁移 0031（三库等价，mysql==mariadb 字节等价；sqlite 版本占位——FTS5 已由 0030 承担，文件仅为版本对齐）：mysql/mariadb `ALTER TABLE search_documents ADD FULLTEXT INDEX search_documents_fts_idx (title, body)`（InnoDB FULLTEXT 原生随行更新，无需触发器，与 SQLite FTS5 触发器策略并列 docs/SEARCH.md §7.1）；分词限制 §7.3：innodb_ft_min_token_size=3（<3 字符 token 不索引不可命中）/innodb_ft_max_token_size=84（>84 字符 token 不索引），服务端全局变量非逐表配置；未启用 ngram parser，CJK 文本按空白/标点分词、长中文串成为最长 84 字符单 token（已知限制，结果层实时可见性过滤兜底 M08-INDEX-07）；重建命令沿用 rebuild_fts（MySQL 分支 `OPTIMIZE TABLE search_documents`）；测试：search_store 新增 #[ignore] mysql_fulltext_index_matches_and_token_limits（需 BBLBB_TEST_MYSQL_URL，CI mysql-family 矩阵 --ignored）：0031 建立索引 + MATCH..AGAINST 命中 fulltext/search + 短 token 'my'(2 字符) 不命中 + 不存在词不命中；ci.yml mysql-family 矩阵追加 `cargo test --test search_store -- --ignored`; commit=4956ba8; review=0031 三库迁移等价 + MySQL FULLTEXT 命中/分词限制测试（CI --ignored）+ ci.yml 接线 + 全量门禁全绿（672 用例 79 套件）
- [x] `M03-SEARCH-STORE-04` `P1` `[45m]` 创建 MariaDB 10.11 FULLTEXT 迁移并记录与 MySQL 的已知差异。证据：files=migrations/mariadb/0031_search_fts.sql,docs/SEARCH.md,docs/SCHEMA.md；commands=cargo fmt --all -- --check 通过; cargo clippy --all-features --all-targets 0 警告; cargo test --all-features 全绿（672 用例 79 套件，上一全量基准，本任务无 Rust 变更）; make check（迁移等价 0031 三库 mysql==mariadb 可执行 SQL 一致、OpenAPI 183/183、write-contract/route-coverage OK、Permission matrix OK + Registry cross-check 68↔38↔38、svelte-check 0 error、事件目录 22/22、TS types 可复现、Roadmap 783 叶子 OK）RC=0; contract=MariaDB 10.11 的 FULLTEXT 迁移与 MySQL 8 共用 0031（可执行 SQL 字节一致，迁移等价测试强制）；差异以注释登记在 mariadb 0031 与 docs/SEARCH.md §7.4：ngram parser（MySQL 8 提供 WITH PARSER ngram / MariaDB 10.11 无同等实现，两者默认按空白/标点分词，长中文串成为 ≤84 字符单 token）；分词上限两者默认一致（innodb_ft_min_token_size=3 / innodb_ft_max_token_size=84，服务端变量）；重建 OPTIMIZE TABLE 两者可用、MariaDB 另支持 ALTER TABLE ... FORCE；内置 InnoDB 停用词表两发行版可能不同，行为只在部署验证中固定（M03-SEARCH-STORE-07 三库 Fixture / M16）；基础查询契约以同一 Fixture 与 MySQL 保持一致; commit=1e414e1; review=mariadb 0031 注释差异 + 迁移等价（注释可不同、可执行 SQL 必须一致）通过 + make check 全绿
- [x] `M03-SEARCH-STORE-05` `P0` `[30m]` 索引写入只接受经过可见性裁决的安全文本，不存 restricted_html。证据：files=backend/src/search/gate.rs,backend/src/search/mod.rs,docs/SEARCH.md；commands=cargo fmt --all -- --check 通过; cargo clippy --all-features --all-targets 0 警告; cargo test --all-features 全绿（gate 单测 5 项，共 677 用例 79 套件）; make check（OpenAPI 183/183、write-contract/route-coverage OK、Permission matrix OK + Registry cross-check 68↔38↔38、svelte-check 0 error、事件目录 22/22、TS types 可复现、迁移等价三库、Roadmap 783 叶子 OK）RC=0; contract=gate.rs（P0）：可见性裁决 decide_post_indexability——唯一可入索引组合 published+public+板块启用且 public+作者 active 且未删除；排除六类：NotPublished（draft/pending_review/rejected/hidden/locked/deleted）、NotPublic（logged_in/after_reply/level/paid 不入公开索引）、BoardInactive、BoardNotPublic（members/restricted/hidden 内容对匿名不可见）、AuthorUnavailable（banned/pending_delete/deleted 或 deleted_at 非空）；decide_user/board/tag_indexability 同理（user active 未删 / board 启用且 public / tag 启用）；安全文本 vet_index_text——拒绝 restricted_html/restricted_markdown 特征串与 HTML 标记（<字母、</），放行纯文本比较符/<3；写路径契约 Indexable→vet 通过→clean_index_text 清洗→SearchDocument::new，受限部分（SCHEMA.md §7 posts/comments restricted_markdown/restricted_html）绝不进入索引输入面；被排除实体从索引移除而非存储受限内容（docs/SEARCH.md §6 登记）; commit=4320dab; review=gate 单测 5 项（post 六类排除全断言/实体 gate/vet 拒绝 restricted 与 HTML 标记/vet 放行比较符与 <3/写契约：Indexable→vet 挡 HTML 与受限泄露）+ 全量门禁全绿（677 用例 79 套件）
- [x] `M03-SEARCH-STORE-06` `P1` `[45m]` 实现创建、更新、隐藏、删除、恢复和退出索引的幂等 Job。证据：files=backend/src/search/index_job.rs,backend/src/search/text.rs,backend/src/search/mod.rs,backend/tests/search_index_job.rs,docs/SEARCH.md；commands=cargo fmt --all -- --check 通过; cargo clippy --all-features --all-targets 0 警告; cargo test --all-features 全绿（text 单测 4 + search_index_job 集成 7，共 688 用例 80 套件）; make check（OpenAPI 183/183、write-contract/route-coverage OK、Permission matrix OK + Registry cross-check 68↔38↔38、svelte-check 0 error、事件目录 22/22、TS types 可复现、迁移等价三库、Roadmap 783 叶子 OK）RC=0; contract=index_job.rs：单一 kind search.index、payload {entity_type, entity_id}，六种语义（创建/更新/隐藏/删除/恢复/退出索引）由源状态裁决推导且全部幂等——写路径=源状态→decide_*_indexability（M03-SEARCH-STORE-05）→to_index_plain_text 剥离 HTML 标签+clean_index_text→vet_index_text（P0 门）→SearchDocument::new→条件 upsert（UPDATE WHERE policy_revision<=candidate；0 行时 INSERT OR IGNORE/INSERT IGNORE——行不存在则插入、陈旧写/并发胜出者不动，旧 revision 不覆盖新）；被排除/源行缺失→DELETE（幂等，FTS 由 0030 触发器同步）；reindex_post 六维裁决（status/visibility/板块启用与可见性/作者状态与 deleted_at）+启用标签入 tags_json；reindex_user/board/tag 各自门；post slug 以 id 占位（M4 加 slug 列）；入队幂等 deduplication_key=search:index:{type}:{id} 合并重复 Job；无效 payload/未知实体类型→永久死信、数据库错误→重试; commit=7a41084; review=search_index_job 集成 7 项（创建入索引+FTS MATCH 命中+标签+幂等重跑 / 隐藏-恢复-删除全链路幂等 / banned 作者退出索引 / 旧 revision 不覆盖新（stored 更大被守卫拒绝） / 重复入队合并为 1 / 无效 payload 永久死信 / board-tag-user 门入出索引）+ text 单测 4 + 全量门禁全绿（688 用例 80 套件）
- [x] `M03-SEARCH-STORE-07` `P1` `[45m]` 用同一 Fixture 验证查询、删除、重建和旧 revision 不覆盖新索引。证据：files=backend/tests/search_fixture.rs,docs/SEARCH.md,.github/workflows/ci.yml；commands=cargo fmt --all -- --check 通过; cargo clippy --all-features --all-targets 0 警告; cargo test --all-features 全绿（sqlite_search_fixture 1 项，共 689 用例 80 套件）; make check（OpenAPI 183/183、write-contract/route-coverage OK、Permission matrix OK + Registry cross-check 68↔38↔38、svelte-check 0 error、事件目录 22/22、TS types 可复现、迁移等价三库、Roadmap 783 叶子 OK）RC=0; contract=search_fixture.rs：同一 fixture_flow 在 SQLite（本地临时库+迁移）与 MySQL 8/MariaDB 10.11（BBLBB_TEST_MYSQL_URL + #[ignore]，CI mysql-family 矩阵 --ignored，分别用 mysql/mariadb 迁移目录）断言一致——查询：帖子经 handle_index_job 入索引后各引擎命中（SQLite FTS5 MATCH / MySQL·MariaDB MATCH..AGAINST NATURAL LANGUAGE）相同；更新：内容变更后旧词不命中、新词命中；删除：源行删除后索引清理且重复执行幂等、查询不再命中；重建：rebuild_fts 两次幂等（行数不变、命中保持，SQLite 走 FTS5 'rebuild'、MySQL 走 OPTIMIZE TABLE）；旧 revision 不覆盖新：stored.policy_revision+100000 后重复执行被条件 upsert 守卫拒绝（stored<=candidate 不成立），值保持不变；入队合并：重复入队 dedup 合并为 1；Fixture 起始/收尾 reset_index（DELETE FROM search_documents）保证 CI 共享库无残留；ci.yml mysql-family 矩阵追加 search_fixture --ignored; commit=1f65bc7; review=sqlite_search_fixture 全链路（查询/更新/删除/重建/旧 revision 守卫/入队合并）通过 + mysql/mariadb 变体编译就绪（CI --ignored）+ 全量门禁全绿（689 用例 80 套件）

## M03-UI：资料、板块和角色前端

**元数据：** `P1` · `owner=frontend-community` · `risk=medium` · `depends=M03-PROFILE,M03-BOARDS` · `blocked=none`
**目标文件：** `frontend/src/routes/users/`、`frontend/src/routes/boards/`、`frontend/src/routes/admin/`、`frontend/tests/`
**验收：** 匿名/member/moderator/admin 的 SSR、键盘、移动端和无 JS 流程通过。

- [x] `M03-UI-01` `P1` `[45m]` 实现用户主页 SSR，处理不存在、匿名化、封禁和资料隐私状态。证据：files=frontend/src/routes/users/[username]/{+page.server.ts,+page.svelte,load.test.ts},frontend/src/lib/testing/ssr/user-page-ssr.test.ts；commands=npm run check（svelte-check 0 error 0 warning，untrack 消除 state_referenced_locally）；npm run test 28 文件 193 用例全绿（load 4 项 + SSR 守卫 2 项 + 客户端隐私 1 项）；npm run build（adapter-node）通过；make check 全绿（OpenAPI 183/183、Roadmap 783 叶子 OK）；make build 后 release 后端全新 SQLite --migrate 启动两次（launch-1/launch-2）healthz 200 "ok"、readyz 200、boards 种子 5 板块、注册 201/重复注册 201/错误密码 401、GET /users/{u} 公开投影 200；前端 adapter-node 服务后 HTTP 加载首页与 /users/bob SSR 200，HTML 含 @ bob/LV.1/个人资料卡且不含 email/session/password_hash，/users/nobody 404 含"用户不存在或已注销"；Playwright 探测不可用记录 launcher-unavailable.log（诚实降级：SSR/结构回退为准）；contract=getPublicUser 公开投影九字段 allowlist 不变（后端 M03-PROFILE 已 implemented），SSR 路径不再泄漏存在性/状态/私有字段；commit=cf2334c；review=load 4 项（成功转发 X-Request-ID/404 不泄漏/5xx→500/降级投影透传）+ SSR 守卫 2 项（对抗性响应不入 DOM/降级安全渲染）+ 端到端 SSR 隐私扫描（首页与用户页无私有字段）+ 启动两次一致性 + 全量门禁全绿
- [x] `M03-UI-02` `P1` `[45m]` 实现资料编辑表单、版本冲突、字段错误和保存后投影刷新。证据：files=frontend/src/routes/settings/{+page.server.ts,+page.svelte,action.test.ts},frontend/src/lib/profile.ts,frontend/src/lib/api/{server.ts,types.ts},frontend/src/lib/testing/ssr/settings-nojs.test.ts；commands=npm run check（svelte-check 0 error 0 warning）；npm run test 30 文件 204 用例全绿（settings action 6 项 + settings-nojs SSR 5 项）；npm run build 通过；make check 全绿（OpenAPI 183/183、Roadmap 783 叶子 OK）；端到端：release 后端 + adapter-node 真实服务——GET /settings SSR 200 含隐藏 version 与持久化字段；POST ?/profile 成功 200 版本 2→3 投影刷新（invalidateAll 同步）；陈旧 If-Match 被后端 409 拒绝、页面渲染"版本冲突/其他窗口被修改"横幅且不落库；curl 需匹配 adapter-node https origin（SvelteKit 同源校验）与 __Host- Secure cookie（浏览器环境天然成立）；contract=updateMe（PATCH /api/v1/me）契约不变：If-Match 必需、409 version_conflict、400/422 字段错误、响应 Me 含 version（M03-PROFILE-04）；User 类型补 version 字段（后端 Me 已返回）；commit=d899786；review=action 6 项（If-Match 版本头转发/超长双端截断/缺版本 422 不调代理/409 conflict 标记/400 透传/503 降级）+ settings-nojs 5 项（原生表单/冲突横幅与最新资料入口/成功投影与版本刷新/对抗性隐私守卫/错误横幅）+ 端到端保存与并发冲突验证 + 全量门禁全绿
- [x] `M03-UI-03` `P1` `[45m]` 实现鼠标 Hover 与键盘 Focus 共用资料卡，支持离开延迟和 Escape 关闭。证据：files=frontend/src/lib/components/{UserCard.svelte,UserCard.test.ts,UserHoverCard.svelte},frontend/src/lib/styles/components.css；commands=npm run check（svelte-check 0 error 0 warning）；npm run test 31 文件 211 用例全绿（UserCard 7 项）；make check 全绿（OpenAPI 183/183、write-contract/route-coverage OK、Permission matrix OK + Registry cross-check 68↔38↔38、事件目录 22/22、TS types 可复现、Roadmap 783 叶子 OK）RC=0；contract=无 OpenAPI 变更；交互契约与 prototype initUserHoverCards 一致（mouseover/focusin 打开、mouseout/focusout 延迟关闭、Escape 关闭、scroll/resize/hashchange 关闭、窄屏不显示浮层）；隐私契约收紧：UserCard/UserHoverCard props 仅 PublicProfile 公开字段 allowlist（username/display_name/level/bio/signature），对抗性数据（邮箱/状态/凭据）不入 DOM；commit=83b3e0f；review=UserCard 7 项（触发为链接 href=/users/{u} 无 JS 可达/打开显示公开字段/离开延迟 250ms 且浮层内停留取消/键盘 Focus 打开 blur 延迟关闭/Escape 立即关闭/隐私 allowlist/缺省头像与自定义 children）+ 现有 privacy SSR 守卫保持通过 + 全量门禁全绿
- [x] `M03-UI-04` `P1` `[30m]` 资料卡使用 portal/fixed 边界，窄屏改为点击/底部卡，不阻挡原导航。证据：files=frontend/src/lib/components/{UserCard.svelte,UserCard.test.ts},frontend/src/lib/styles/components.css,frontend/src/lib/testing/ssr/usercard-nojs.test.ts；commands=npm run check（svelte-check 0 error 0 warning）；npm run test 32 文件 217 用例全绿（UserCard 11 项 + usercard-nojs SSR 2 项）；npm run build（adapter-node）通过；make check 全绿（OpenAPI 183/183、write-contract/route-coverage OK、Permission matrix OK、事件目录 22/22、Roadmap 783 叶子 OK）RC=0；contract=无 OpenAPI 变更；交互契约：桌面浮层 mount() portal 到 document.body（Svelte 5 unmount 销毁，避免 transform/overflow 祖先裁剪 fixed 定位）、视口边缘夹紧；窄屏（≤640px，prototype 同断点）点击触发改底部卡 .user-card-sheet + 关闭按钮，再次点击/Escape/滚动关闭，无全屏遮罩；触发元素恒为 <a>（无 JS 直接跳主页，不阻挡原导航）；SSR 只输出触发链接，浮层与私有字段绝不出现在 HTML；commit=088453d；review=UserCard 11 项（原 M03-UI-03 7 项保持 + portal 宿主在 body + 窄屏点击出卡无遮罩/再次点击与 Escape 关闭/关闭按钮）+ usercard-nojs 2 项（SSR 仅触发链接可跳转、对抗性字段不入 HTML）+ 全量门禁全绿
- [x] `M03-UI-05` `P0` `[30m]` 资料卡和主页不持久化签名 URL或私密字段，Cover 失败时安全降级。证据：files=frontend/src/lib/components/ui/{ProfileCover.svelte,ProfileCover.test.ts},frontend/src/lib/components/{UserHoverCard.svelte,UserCard.svelte,UserCard.test.ts},frontend/src/routes/users/[username]/+page.svelte,frontend/src/lib/testing/{ssr/user-page-ssr.test.ts,user-page-privacy.test.ts},frontend/src/lib/styles/components.css；commands=npm run check（svelte-check 0 error 0 warning）；npm run test 33 文件 220 用例全绿（ProfileCover 3 项 + 扩展隐私守卫）；make check 全绿（OpenAPI 183/183、Error codes OK 46/46、write-contract/route-coverage OK、Permission matrix OK、事件目录 22/22、TS types 可复现、Roadmap 783 叶子 OK）RC=0；contract=无 OpenAPI 变更；隐私契约：页面/资料卡数据只携带附件引用（avatar/cover_attachment_id），签名 URL 由 M6 渲染期临时解析——不进入组件状态、不写 data-* 属性、不缓存 localStorage、不进入 SSR HTML/客户端 DOM（对抗性签名 URL cdn/X-Amz-Signature/Expires/avatar_url/cover_url/signed_url 全部断言不泄漏）；ProfileCover 缺省渐变占位、img 加载失败安全降级（移除 img + has-error）、装饰性 aria-hidden；commit=6410831；review=ProfileCover 3 项（无 src 无 URL 占位/加载失败降级无破图/aria 语义）+ 三处对抗性隐私守卫扩展（用户页 SSR、用户页客户端缓存、UserCard 浮层均断言签名 URL 与私密字段不泄漏）+ 全量门禁全绿
- [x] `M03-UI-06` `P1` `[45m]` 实现板块树、板块详情、标签筛选、空状态和权限提示。证据：files=frontend/src/routes/{boards/+page.server.ts,boards/+page.svelte,boards/[slug]/{+page.server.ts,+page.svelte},tags/{+page.server.ts,+page.svelte},search/+page.svelte},frontend/src/lib/components/Navbar.svelte,backend/src/routes/boards.rs,backend/tests/boards_no_leak.rs,openapi/openapi.yaml,frontend/src/lib/api/{types.ts,generated/v1/*},frontend/src/lib/testing/ssr/boards-tags-nojs.test.ts；commands=make check 全绿（cargo fmt/clippy 0 警告、OpenAPI 183/183、Error codes 46/46、write-contract/route-coverage OK、Permission matrix OK 68↔38↔38、svelte-check 0 error 0 warning、事件目录 22/22、TS types 可复现、Roadmap 783 叶子 OK）RC=0；cargo test --all-features 82 套件 0 失败（boards_no_leak 5 项含新增 authenticated_projection）；npm run test 37 文件 234 用例（boards/detail/tags load 7 项 + boards-tags-nojs SSR 7 项）；端到端：release 后端+adapter-node——匿名 /boards SSR 5 板块卡且无 visibility/parent_id/post_count 泄漏、认证 SSR __data.json 含权限字段、/boards/tech 200、/boards/ghost 404、/tags 空状态、/search?tag= 筛选 chip、Navbar 标签链接；contract=OpenAPI Board schema 增补 parent_id/visibility/posting_mode/post_count/is_active（仅已认证投影返回，M03-BOARDS-08 匿名最小投影保持）；getBoard/listBoards 覆盖保持 implemented；/boards/{slug}/posts 增 author_name；commit=7c1199c；review=前端 load 7 项（转发会话/404 不泄漏/错误降级）+ SSR 7 项（板块树分组/子板块计数/权限徽标/详情提示 members+readonly/空状态/标签分组与 ?tag= 链接/隐私）+ 后端 no_leak 5 项（匿名最小投影保持 + 已认证投影含可见性且隐藏父级不泄漏）+ 端到端匿名/认证投影差异验证 + 全量门禁全绿
- [x] `M03-UI-07` `P1` `[45m]` 实现管理员板块、标签、角色和 assignment 页面，所有拒绝仍由后端裁决。证据：files=frontend/src/routes/admin/{+layout.svelte,+page.svelte,boards/{+page.server.ts,+page.svelte,load.test.ts},tags/{+page.server.ts,+page.svelte},roles/{+page.server.ts,+page.svelte},assignments/{+page.server.ts,+page.svelte}},frontend/src/lib/admin.ts,frontend/src/lib/testing/ssr/admin-nojs.test.ts,scripts/check-state-enums.rb；commands=npm run check（svelte-check 0 error 0 warning）；npm run test 39 文件 246 用例全绿（admin load 5 项 + admin-nojs SSR 7 项）；make check 全绿（cargo fmt/clippy 0 警告、OpenAPI 183/183、Error codes 46/46、write-contract/route-coverage OK、Permission matrix OK、事件目录 22/22、TS types 可复现、Roadmap 783 叶子 OK）RC=0；端到端：/admin 着陆渲染 4 入口（板块/标签/角色/Assignment）、匿名 /admin/boards 渲染 501 开发中态且创建表单可提交（reason 审计字段）；contract=无 OpenAPI 变更；授权契约：AdminLoadState 状态机 401→登录 303、403→无权限态、501→开发中态、5xx→错误，前端不自行判权（所有拒绝由后端裁决）；创建表单走已实现 POST /api/v1/admin/boards|tags（board.manage/tag.manage 权限门 + reason 审计，M03-BOARDS-05）；列表接口 501（M13-ADMIN 落地）；check-state-enums.rb 修正——status=closed 弃用约束按 /status 字段精确判定，posting_mode 的 closed（板块关闭发帖）为合法稳定枚举（STATE-MACHINES.md §Board），负向注入验证仍失败；commit=2112ff7；review=admin load 5 项（200 ok/403 forbidden 后端裁决/501 not_implemented/401→303 跳登录/表单缺失 422 不调代理）+ admin-nojs SSR 7 项（ok 列表/not_implemented 开发中+创建表单/403 无权限/error 文案/角色列表 scope+权限/Assignment 契约说明）+ 状态机检查器负向验证 + 全量门禁全绿
- [x] `M03-UI-08` `P1` `[45m]` 测试键盘、触屏、减少动效、无 JS 公开资料/板块浏览和响应式布局。证据：files=frontend/src/lib/testing/{a11y-interactions.test.ts,ambient-fs.d.ts,a11y.ts,a11y.test.ts,ssr/{user-page-ssr.test.ts,boards-tags-nojs.test.ts,usercard-nojs.test.ts,settings-nojs.test.ts,me-nojs.test.ts}},frontend/src/app.d.ts；commands=npm run check（svelte-check 0 error 0 warning）；npm run test 40 文件 254 用例全绿（a11y-interactions 8 项新增）；make check 全绿（cargo fmt/clippy 0 警告、OpenAPI 183/183、write-contract/route-coverage OK、Permission matrix OK、事件目录 22/22、TS types 可复现、Roadmap 783 叶子 OK）RC=0；contract=无 OpenAPI 变更；验收覆盖：键盘（板块总览 Tab 遍历序 DOM 序、UserCard Focus 打开 Escape 关闭）；触屏（窄屏触摸点击底部卡无遮罩不挡导航、桌面触摸保持原导航）；减少动效（已发布 CSS components/base 含 @media prefers-reduced-motion 降级——transition/animation 关闭或时长 0.01ms，结构守卫 + 偏好下组件渲染不抛错）；无 JS 公开资料/板块浏览（既有 SSR 测试覆盖：用户主页/板块树/详情/标签/资料卡/设置/我的，SSR HTML 可读且无私有字段泄漏）；响应式（≤640px 断点切换 hover 浮层↔底部卡、窄屏板块卡不丢内容）；commit=1050ac1；review=a11y-interactions 8 项（Tab 序含面包屑先于板块链接/键盘 Escape 关闭/触摸两态/减少动效结构守卫+渲染/断点切换/窄屏内容完整）+ 既有无 JS SSR 套件 + 全量门禁全绿
- [x] `M03-UI-09` `P1` `[30m]` 将原型对应路由映射到生产路由矩阵，不导入 mock/store 数据源。证据：files=frontend/src/lib/route-matrix.ts,frontend/src/lib/testing/{route-matrix.test.ts,ambient-fs.d.ts},docs/FRONTEND.md；commands=npm run check（svelte-check 0 error 0 warning）；npm run test 41 文件 258 用例全绿（route-matrix 4 项新增）；make check 全绿（cargo fmt/clippy 0 警告、OpenAPI 183/183、Error codes 46/46、write-contract/route-coverage OK、Permission matrix OK、事件目录 22/22、TS types 可复现、Roadmap 783 叶子 OK——汇总 182 完成/0 进行中/2 阻塞/599 未开始）RC=0；contract=无 OpenAPI 变更；路由契约：PROTOTYPE_ROUTE_MATRIX 一一覆盖 prototype/js/router.js 全部 47 路由（42 静态 + 5 动态：boards/tags/topics/users/admin-reports），status shipped=生产已存在 +page 路由（结构测试遍历 src/routes 断言 +page.svelte/+error.svelte 存在，[param] 归一化 {param}，查询形态 /search?tag= 跳过，/403|/404|/429→+error.svelte），planned=标注交付里程碑（M4/M5/M7/M13，M14-ROUTES-07 由 M13-ADMIN 落地）；生产源码不导入原型 mock/store/脚本数据源（import 语句守卫 + 单行引号包裹的原型路径防御，描述性注释提及原型出处除外）；docs/FRONTEND.md §3 增加路由矩阵说明（新增/改动生产路由需同步矩阵）；commit=70f507d；review=route-matrix 4 项（矩阵覆盖全部原型路由且排序无关集合相等无多余行/shipped 路由磁盘存在——/boards/{slug} 与 /users/{username} 归一化、/+error.svelte 与查询形态分支/生产源码无原型导入/planned 均带 M 里程碑）+ svelte-check 0/0 + 全量门禁全绿

---

<a id="m4"></a>

# M4：Markdown、帖子、文章、回复与可见性

**完成定义：** article/discussion/draft/comment 全链路可用；Markdown 安全渲染；隐藏正文不会从任何非授权投影泄漏；并发更新有稳定冲突语义。

## M04-SCHEMA：内容与版本数据模型

**元数据：** `P0` · `owner=backend-db` · `risk=critical` · `depends=M03-AUTHZ,M03-BOARDS` · `blocked=none`
**目标文件：** `migrations/*/`、`backend/src/content/model*`、`docs/SCHEMA.md`
**验收：** 三数据库约束、楼层并发、revision 和软删除 repository contract 通过。

- [x] `M04-SCHEMA-01` `P0` `[45m]` 新增 posts，覆盖 article/discussion、board、author、slug、状态、revision 和发布时间。证据：files=migrations/{sqlite,mysql,mariadb}/0032_posts_metadata.sql,backend/src/content/{mod.rs,model.rs,repository.rs},backend/src/lib.rs,backend/tests/posts_schema.rs,docs/SCHEMA.md；commands=cargo test --all-features（703 通过/0 失败，含 posts_schema 5 项新增 + model 单测 3 项）；cargo fmt --check 通过；cargo clippy --all-features --all-targets 0 警告；make check 全绿（cargo fmt/clippy 0 警告、OpenAPI 183/183、Error codes 46/46、write-contract/route-coverage OK、Permission matrix OK、migration_equivalence 三库等价含 0032、事件目录 22/22、TS types 可复现、Roadmap 783 叶子 OK）RC=0；contract=迁移 0032（三库同名）posts 增列：post_type（article/discussion CHECK）、slug（板块内唯一 UNIQUE(board_id,slug)，多 NULL 不冲突）、excerpt、version（乐观并发，If-Match 来源）、scheduled_at/published_at/pinned_at/featured_at/closed_at（锁帖独立于发布状态）、canonical_url/seo_title/seo_description、last_reply_id、deleted_at（软删）；新增索引 (board_id,status,pinned_at,last_reply_at)/(author_id,status,created_at)/(post_type,status,published_at)/(scheduled_at)；骨架遗留列 content/content_format/visibility/pinned/last_reply_by 保留至 M04-POSTS 收口（搜索/骨架路由仍引用）；status CHECK 保持 0003 值域（locked 为遗留值，closed_at 取代；pending_review/rejected 随 M04-POSTS 迁移扩展——SQLite 改 CHECK 需重建表）；PostType/PostStatus/Post 模型 + insert/get 仓储（三库 Either 一致）；comments(post_id→posts.id) 外键不受影响；commit=6d797e2；review=posts_schema 5 项（0032 元数据列存在+骨架遗留列保留/板块内 slug 唯一：同板块冲突、跨板块允许、多 NULL 草稿允许/post_type CHECK 拒绝未知、status 四稳定值可写/仓储往返含发布时间版本视图计数/replies_open 锁帖语义/comments 外键完整）+ model 单测 3 项（枚举往返+遗留 locked 解析/replies_open）+ migration_equivalence 三库等价 + 全量门禁全绿
- [x] `M04-SCHEMA-02` `P0` `[45m]` 新增 post_contents/revisions，保存 Markdown、清洗 HTML、renderer version 和安全摘要。证据：files=migrations/{sqlite,mysql,mariadb}/0033_post_contents.sql,backend/src/content/{model.rs,repository.rs},backend/tests/post_contents_schema.rs,docs/SCHEMA.md；commands=cargo test --all-features（707 通过/0 失败，含 post_contents_schema 4 项新增）；cargo fmt --check 通过；cargo clippy --all-features --all-targets 0 警告；make check 全绿（cargo fmt/clippy 0 警告、OpenAPI 183/183、migration_equivalence 三库等价含 0033、事件目录 22/22、Roadmap 783 叶子 OK）RC=0；contract=迁移 0033（三库同名）：post_contents 与 posts 1:1（post_id 主键 + 级联删除）——body_markdown（原文）、body_html（后端清洗后公开 HTML）、restricted_markdown/restricted_html（可空）、renderer_version（M04-MARKDOWN-05 升级触发重渲染）、excerpt（公开安全摘要，禁止从隐藏正文截断，M04-MARKDOWN-06）、updated_at；post_revisions 不可变快照——id/post_id/editor_id/正文与受限正文 Markdown+HTML 快照/renderer_version/change_reason/version（对应 posts.version）/created_at，UNIQUE(post_id,version) 每版恰好一条 + (post_id,version) 与 editor_id 索引 + 随 post 级联删除；仓储 save_post_content（upsert 覆盖不产生重复行）/load_post_content/insert_post_revision（同版冲突）/list_post_revisions（version 升序）；commit=6979c15；review=post_contents_schema 4 项（正文往返含受限正文/renderer version/摘要/1:1 upsert 单行/修订不可变+有序+同版唯一/级联删除清空 contents 与 revisions）+ migration_equivalence 三库等价 + 全量门禁全绿
- [x] `M04-SCHEMA-03` `P0` `[30m]` 新增 drafts 与 scheduled_at，并为 owner、更新时间和 cursor 建立索引。证据：files=migrations/{sqlite,mysql,mariadb}/0034_drafts.sql,backend/src/content/{model.rs,repository.rs},backend/tests/drafts_schema.rs,docs/SCHEMA.md；commands=cargo test --all-features（711 通过/0 失败，含 drafts_schema 4 项新增）；cargo fmt --check 通过；cargo clippy --all-features --all-targets 0 警告；make check 全绿（cargo fmt/clippy 0 警告、OpenAPI 183/183、migration_equivalence 三库等价含 0034、事件目录 22/22、Roadmap 783 叶子 OK）RC=0；contract=迁移 0034（三库同名）：drafts 独立草稿资源（OpenAPI Draft）——owner_id/board_id（可空，board 删除 SET NULL 不级联）/post_type（article/discussion CHECK）/title/markdown（原文，发布时渲染进 post_contents）/visibility_level/access_policy（发布预设）/scheduled_at（定时发布）/version（乐观并发）/created_at/updated_at/deleted_at（软删行保留）；索引 (owner_id,deleted_at,updated_at)（owner cursor keyset 分页）+ (scheduled_at)（定时发布 Job 扫描）；仓储 insert_draft/get_draft（owner 隔离：非 owner 读不到）/update_draft（version+1）/delete_draft（软删）/list_drafts_cursor（updated_at 降序 + before keyset）/list_scheduled_drafts（scheduled_at<=due）；commit=99367db；review=drafts_schema 4 项（插入+owner 隔离/有序分页 cursor 列表+d5,d4,d3→d2,d1→空/更新 version 递增+软删隐藏但行保留/定时扫描只返回到期+board 删除置空）+ migration_equivalence 三库等价 + 全量门禁全绿
- [x] `M04-SCHEMA-04` `P0` `[45m]` 新增 comments、parent_id、floor、revision、状态和软删除字段。证据：files=migrations/{sqlite,mysql,mariadb}/0035_comments_metadata.sql,backend/src/content/{model.rs,repository.rs},backend/tests/comments_schema.rs,docs/SCHEMA.md；commands=cargo test --all-features（715 通过/0 失败，含 comments_schema 4 项新增）；cargo fmt --check 通过；cargo clippy --all-features --all-targets 0 警告；make check 全绿（cargo fmt/clippy 0 警告、OpenAPI 183/183、migration_equivalence 三库等价含 0035、事件目录 22/22、Roadmap 783 叶子 OK）RC=0；contract=迁移 0035（三库同名）：comments 增列 quoted_comment_id（引用回复，删除 SET NULL 保留"已删除"占位语义，sqlite ADD COLUMN REFERENCES / mysql ADD CONSTRAINT FK）+ version（乐观并发）+ deleted_at（软删行保留供占位投影与审计）；parent_id/floor 为既有列（floor=主题内楼层号，唯一约束随 M04-SCHEMA-07）；content/content_format 为骨架遗留正文列（M04-COMMENTS 收口）；status CHECK 保持 0003（published/hidden/deleted；pending 随 M04-POSTS 扩展）；CommentStatus 枚举 + Comment 模型（floor/parent/quoted/version/软删）；仓储 insert_comment（楼层号调用方事务内分配，M04-COMMENTS-03）/get_comment/list_comments_by_post（floor 升序）/update_comment（version+1）/delete_comment（软删）；commit=d47e176；review=comments_schema 4 项（0035 列存在+parent/quoted/floor/version 往返/floor 升序列表/更新 version 递增+软删保留行/引用删除置空不级联删除引用方）+ migration_equivalence 三库等价 + 全量门禁全绿
- [x] `M04-SCHEMA-05` `P0` `[30m]` 新增 post/tag、附件引用、封面引用和引用回复关联。证据：files=migrations/{sqlite,mysql,mariadb}/0036_post_relations.sql,backend/src/content/{model.rs,repository.rs},backend/tests/post_relations_schema.rs,docs/SCHEMA.md；commands=cargo test --all-features（718 通过/0 失败，含 post_relations_schema 3 项新增）；cargo fmt --check 通过；cargo clippy --all-features --all-targets 0 警告；make check 全绿（cargo fmt/clippy 0 警告、OpenAPI 183/183、migration_equivalence 三库等价含 0036、事件目录 22/22、Roadmap 783 叶子 OK）RC=0；contract=迁移 0036（三库同名）：posts.cover_attachment_id（封面附件 UUID 引用，attachments 表 M6 落地后补 FK，只存 UUID 禁存远程/签名 URL，与 users.cover_attachment_id 一致）+ post_tags.created_at（标签列表排序，默认 0）+ post_attachments（id/post_id/attachment_id/kind cover|gallery CHECK/position 渲染顺序/created_at，随 post 级联删除）；引用回复关联 quoted_comment_id 由 M04-SCHEMA-04 落地（本任务登记）；AttachmentKind/PostAttachment/PostTag 模型 + insert_post_tag（复合主键防重）/list_post_tags（created_at 排序）/insert_post_attachment/list_post_attachments（position 排序）/set_post_cover；commit=1acea41；review=post_relations_schema 3 项（封面引用往返+清空/post_tags 排序+重复拒绝+级联删除/post_attachments kind CHECK+position 排序+级联删除）+ migration_equivalence 三库等价 + 全量门禁全绿
- [x] `M04-SCHEMA-06` `P0` `[45m]` 新增 access policy：public/logged_in/after_reply/level/paid 及策略版本。证据：files=migrations/{sqlite,mysql,mariadb}/0037_access_policy.sql,backend/src/content/{model.rs,repository.rs},backend/tests/access_policy_schema.rs,docs/SCHEMA.md；commands=cargo test --all-features（720 通过/0 失败，含 access_policy_schema 2 项新增）；cargo fmt --check 通过；cargo clippy --all-features --all-targets 0 警告；make check 全绿（cargo fmt/clippy 0 警告、OpenAPI 183/183、migration_equivalence 三库等价含 0037、事件目录 22/22、Roadmap 783 叶子 OK）RC=0；contract=迁移 0037（三库同名）：content_access_policies——kind 封闭枚举 public/logged_in/after_reply/level/paid（CHECK，与 posts.visibility 遗留值域一致，M04-VISIBILITY-01 形式化）/min_level（level 策略）/currency_id+amount（paid 策略，金额必须为正）/reply_grant_persists（after_reply 回复删除后授权保留，M04-VISIBILITY-05 冻结规则）/policy_version（策略版本，默认 1）/created_by+created_at（审计）；posts.access_policy_id 可空外键（未设=public，策略删除 SET NULL 回退 public）；ContentAccessPolicy 模型 + validate() 结构校验（level 需 min_level/paid 需 currency+amount 且为正）+ insert/get/set_post_access_policy；commit=910a92b；review=access_policy_schema 2 项（三种简单 kind 往返+结构校验矩阵 level/paid 错误与通过/kind CHECK 拒绝未知/帖子关联往返+清除+策略删除置空回退 public）+ migration_equivalence 三库等价 + 全量门禁全绿
- [x] `M04-SCHEMA-07` `P0` `[30m]` 建立板块内 slug、主题内楼层、客户端请求 ID 和 revision 唯一约束。证据：files=migrations/{sqlite,mysql,mariadb}/0038_comments_floor_uq.sql,backend/tests/content_unique_constraints.rs,docs/SCHEMA.md；commands=cargo test --all-features（722 通过/0 失败，含 content_unique_constraints 2 项新增）；cargo fmt --check 通过；cargo clippy --all-features --all-targets 0 警告；make check 全绿（cargo fmt/clippy 0 警告、OpenAPI 183/183、migration_equivalence 三库等价含 0038、事件目录 22/22、Roadmap 783 叶子 OK）RC=0；contract=四类唯一约束齐备并验证：板块内 slug=posts UNIQUE(board_id,slug)（0032 posts_board_slug_uq，跨板块允许、多 NULL 草稿不冲突）；主题内楼层=comments UNIQUE(post_id,floor)（0038 新增，同主题同楼层拒绝、跨主题允许，M04-COMMENTS-03 事务内原子分配兜底）；revision=post_revisions UNIQUE(post_id,version)（0033，每版恰好一条）；客户端请求 ID=idempotency_records UNIQUE(scope,key)（0010，client_request_id 以 scope="post.create"/"draft.create" 写入，M04-POSTS-03 幂等）；commit=0aeb13d；review=content_unique_constraints 2 项（同主题同楼层拒绝+跨主题允许；同板块同 slug 拒绝+同版修订拒绝+同 (scope,key) 幂等记录拒绝）+ 既有 posts_schema/post_contents_schema/comments_schema 唯一性断言保持 + migration_equivalence 三库等价 + 全量门禁全绿
- [x] `M04-SCHEMA-08` `P0` `[45m]` 测试并发楼层、slug 冲突、非法 parent、孤儿附件引用和软删恢复。证据：files=backend/src/content/model.rs,backend/tests/content_concurrency_schema.rs；commands=cargo test --all-features（726 通过/0 失败，含 content_concurrency_schema 4 项新增）；cargo fmt --check 通过；cargo clippy --all-features --all-targets 0 警告；make check 全绿（cargo fmt/clippy 0 警告、OpenAPI 183/183、事件目录 22/22、Roadmap 783 叶子 OK）RC=0；contract=并发楼层：同主题同楼层 tokio::join 并发插入恰好一个成功（UNIQUE(post_id,floor) 兜底 M04-COMMENTS-03 事务内原子分配）；slug 冲突：同板块同 slug 并发恰好一个成功（UNIQUE(board_id,slug)）；非法 parent：Comment::validate_quote_scope 校验 parent/quote 必须与自身同主题（跨主题返回稳定错误，M04-COMMENTS-02 防泄漏），DB 外键 parent_id→comments 保证存在性；孤儿附件引用：帖子删除级联清除 post_attachments/post_tags/post_revisions（不留孤儿行）；软删恢复：deleted_at 置位后行保留、清除即恢复；commit=79f956f；review=content_concurrency_schema 4 项（并发楼层一胜/并发 slug 一胜/跨主题 parent 拒绝+同主题通过/删除不留孤儿+软删恢复）+ 全量门禁全绿
- [x] `M04-SCHEMA-09` `P1` `[30m]` 同步 Schema、状态机、事件目录和 OpenAPI schema 差异。证据：files=docs/{SCHEMA.md,STATE-MACHINES.md},docs/EVENT-CATALOG.md,openapi/openapi.yaml；commands=ruby scripts/check-openapi.rb（OpenAPI OK 183 操作、AccessSummary.policy 枚举=DB kind 枚举 public/logged_in/after_reply/level/paid、Manifest 183/183）；ruby scripts/check-state-enums.rb（PostType/PostStatus/CommentStatus/AttachmentKind 入 Rust domain enums 识别，无手写 TS 枚举）；ruby scripts/check-roadmap.rb（Roadmap OK 783 叶子）；make check 全绿（cargo fmt/clippy 0 警告、OpenAPI 183/183、事件目录 22/22、TS types 可复现、Roadmap 783 叶子 OK）RC=0；contract=SCHEMA.md 全章节同步（posts 元数据+兼容说明/post_contents/revisions/drafts/comments/post_tags|post_attachments/content_access_policies/四类唯一约束）；STATE-MACHINES.md Post 节补充 DB CHECK 值域同步说明（locked 遗留值、pending_review/rejected 随 M04-POSTS 扩展，closed_at 锁帖语义）；EVENT-CATALOG 无变更（SCHEMA 任务不引入领域事件，事件随 POSTS/COMMENTS 服务落地）；OpenAPI Post/AccessSummary 契约已与 DB 模型对齐（post_type 枚举、access_summary.policy 五值）；commit=60f6dae；review=同步门禁全绿（check-openapi/check-state-enums/check-roadmap/make check）+ 文档一致性核对（Schema 与 DB DDL、状态机与 DB CHECK、OpenAPI 枚举与 Rust 枚举）

## M04-MARKDOWN：安全渲染管线

**元数据：** `P0` · `owner=backend-security` · `risk=critical` · `depends=M04-SCHEMA` · `blocked=none`
**目标文件：** `backend/src/content/markdown/`、`backend/tests/markdown/`、`frontend/src/lib/components/SafeHtml.svelte`
**验收：** XSS corpus、链接/图片策略、renderer version 和前端 sink 扫描通过。

- [x] `M04-MARKDOWN-01` `P0` `[30m]` 请求只接受 Markdown；显式拒绝原始 HTML、BBCode 和未知内容格式。证据：files=backend/src/domain/content.rs,backend/src/domain/mod.rs；commands=cargo test --lib domain::content（6 通过/0 失败）；cargo fmt --check 通过；cargo clippy --lib -- -D warnings 0 警告；make check 全绿（cargo fmt/clippy 0 警告、OpenAPI 183/183、事件目录 22/22、Roadmap 783 叶子 OK）RC=0；contract=ContentFormat 唯一可接受值 markdown（空/缺省按 markdown 兼容骨架；大小写与空白不敏感）；ContentFormatRejected 结构化拒绝（RawHtml=html|text/html|xhtml、Bbcode=bbcode|bbc、Unknown=其余）供路由层映射稳定 Problem detail；错误消息一律不含原始输入（防回显 XSS/注入）；ACCEPTED 单一事实来源；commit=b003767；review=6 项单测（仅 markdown 通过+缺省兼容/HTML 显式拒绝含 text/html 与大小写/BBCode 显式拒绝/未知格式分类/错误消息稳定且无原始输入回显/ACCEPTED 与 as_str 一致）+ 全量门禁全绿
- [x] `M04-MARKDOWN-02` `P0` `[45m]` 选择并封装 CommonMark 渲染器，禁用原始 HTML 和危险扩展。证据：files=backend/Cargo.toml,backend/Cargo.lock,backend/src/content/markdown/{mod.rs,render.rs,policy.rs}；commands=cargo test --lib content::markdown（17 通过/0 失败，含 render 6 项）；cargo test --lib（288 通过/0 失败）；cargo fmt --check 通过；cargo clippy --lib -- -D warnings 0 警告；make check 全绿 RC=0；contract=渲染管线封装于 backend/src/content/markdown：pulldown-cmark 0.10（feature html）+ render_to_html 使用 render_options=仅 ENABLE_TABLES（footnotes/math/YAML/GFM 危险扩展全部关闭）；序列化前过滤 Event::Html/Event::InlineHtml（原始 HTML 不进入输出，双保险）；MAX_INPUT_CHARS=50_000 防御性截断（超长输入按 char 截断后渲染，Cow 避免拷贝）；render_and_sanitize=render→sanitize 唯一入口；RENDERER_VERSION=markdown-v1 登记于 policy.rs；commit=7e97e3b；review=render 6 项单测（标准 Markdown 输出/原始 HTML 事件被剥离/危险扩展关闭断言/footnotes|math|strikethrough 不生效/表格渲染/超长输入截断不 panic）+ sanitize 与 excerpt 联动测试 + 全量门禁全绿
- [x] `M04-MARKDOWN-03` `P0` `[45m]` 建立标签、属性、协议、图片、外链 rel/target 和 iframe Provider allowlist。证据：files=backend/src/content/markdown/{sanitize.rs,policy.rs}；commands=cargo test --lib content::markdown（17 通过/0 失败，含 sanitize 7 项）；cargo test --lib（288 通过/0 失败）；cargo fmt --check 通过；cargo clippy --lib -- -D warnings 0 警告；make check 全绿 RC=0；contract=ammonia 4.1.4 白名单策略：标签=段落/标题/列表/表格/引用/代码/a/img/strong|em|del|s|b|i|u/span/figure|figcaption/iframe 等；属性=仅 a[href,title]、img[src,alt,title,width,height,loading]、code|pre[class]（仅 language-*）、iframe[src,title,width,height,loading,allowfullscreen]、th|td[align,colspan,rowspan]；url_schemes=["http","https","mailto"]、url_relative=Deny（协议相对与相对 URL 均拒绝）；外链 a 强制 rel="nofollow noopener noreferrer"+target="_blank"（link_rel 与 tag 属性白名单不冲突）；iframe src 主机白名单=www.youtube.com/www.youtube-nocookie.com/player.vimeo.com/player.bilibili.com（attribute_filter 拒绝白名单外）；SANITIZER_VERSION=ammonia-v1 登记于 policy.rs；commit=7e97e3b；review=sanitize 7 项单测（白名单标签保留+黑名单剥离/事件属性 on* 剥离/javascript|data URL 拒绝/相对 URL 拒绝/rel+target 强制注入/iframe 提供商白名单内外对比/code class 仅 language-*）+ 全量门禁全绿
- [x] `M04-MARKDOWN-04` `P0` `[30m]` 对标题锚点、代码块、引用、表格和超长嵌套设置确定性输出和上限。证据：files=backend/src/content/markdown/{render.rs,policy.rs}；commands=cargo test --lib content::markdown（28 通过/0 失败，含 render 17 项）；cargo test --lib（299 通过/0 失败）；cargo fmt --check 通过；cargo clippy --lib -- -D warnings 0 警告；make check 全绿 RC=0；contract=确定性输出与上限全部落地于渲染管线（policy.rs 登记常量）：标题锚点=确定性 slug id（Unicode alphanumeric 保留含中文、非字母数字折叠为 `-`、64 字符截断、同文档去重 `-1/-2`，纯符号标题回退 `section`），同一输入永远同一输出；代码块=单块 20k char 截断保留开头；blockquote 深度上限 10（超层引用对展平、内容保留）；块级容器（引用+列表）总深度上限 64、行内嵌套上限 16（防御兜底，展平语义）；表格列数上限 20（超出列整格丢弃、行结构保留）+ 单格 5k char 截断；渲染总量估算上限 300k（兜底停止继续渲染）；以上裁剪全部只依赖输入事件、确定性可复现；commit=98a7aac；review=render 新增 11 项单测（标题 slug 确定性/中文锚点/重复标题去重/纯符号回退/代码块截断有界/引用深度恰为 10 且内容保留/100 层列表展平/30 列表格裁剪到 20 且越界内容丢弃/超长单元格截断/合成事件流验证行内嵌套裁剪+普通强调正常/同一输入两次渲染逐字节相等）+ 既有 17 项保持 + 全量门禁全绿
- [x] `M04-MARKDOWN-05` `P0` `[30m]` 保存 renderer/sanitizer policy version，升级时通过 Job 重渲染旧 revision。证据：files=backend/src/content/markdown/{rerender.rs,policy.rs},backend/src/content/repository.rs,backend/tests/markdown_rerender.rs,docs/JOBS.md；commands=cargo test --lib content::markdown（32 通过/0 失败）；cargo test --test markdown_rerender（4 通过/0 失败）；cargo test --lib（303 通过/0 失败）；cargo fmt --check 通过；cargo clippy --lib --tests -- -D warnings 0 警告；make check 全绿 RC=0；make test 全绿 RC=0；contract=策略版本持久化：policy_version()=renderer+sanitizer 组合版本（renderer 或 sanitizer 任一升级即改变值）写入 post_contents/post_revisions.renderer_version（写路径经 render_content 统一产出 body_html/restricted_html/excerpt/renderer_version 四件套）；升级重渲染：markdown.rerender Job kind（payload {target:content|revision, id}、dedup key markdown:rerender:{target}:{id} 幂等合并），enqueue_rerender_jobs 扫描 renderer_version<>当前 的 post_contents+post_revisions 每行入队一个 Job；handle_rerender_job=worker 集成入口：当前策略重渲染并覆盖渲染产物（markdown 快照/editor/version/created_at 不变）、行缺失或已最新→幂等成功、无效 payload→永久死信、DB 错误→瞬时重试；新增 get_post_revision/update_post_revision_rendered 仓储；sanitize 放行 h1..h6[id]（锚点仅来自后端确定性 slug 生成器，事件属性仍剥离）；docs/JOBS.md 登记 job kind；commit=2381f1a；review=单测 5 项（版本组合/渲染产物带当前版本/无受限部分保持 None/锚点 id 过清洗保留+事件属性仍剥离）+ 集成测试 4 项（升级后 2 行入队+dedup 合并+重渲染内容与修订断言 markdown 快照不变/重复入队 0 新增；缺失行幂等成功；已最新行 no-op；无效 payload 矩阵永久死信）+ make check/test 全绿
- [x] `M04-MARKDOWN-06` `P0` `[45m]` 生成公开摘要时先执行可见性和 Markdown 安全处理，禁止从隐藏正文截断。证据：files=backend/src/content/markdown/excerpt.rs,backend/src/content/markdown/rerender.rs,backend/tests/markdown_rerender.rs；commands=cargo test --lib content::markdown（36 通过/0 失败，含 excerpt 8 项）；cargo test --test markdown_rerender（4 通过/0 失败）；cargo test --lib（307 通过/0 失败）；cargo fmt --check 通过；cargo clippy --lib --tests -- -D warnings 0 警告；make check 全绿 RC=0；make test 全绿 RC=0；contract=render_public_excerpt(body_markdown, restricted_markdown) 为投影层展示摘要的单一入口：先判定可见性——摘要只从公开正文生成，隐藏正文（restricted）无论多长、即使公开正文为空或仅空白，都绝不参与截断与拼接（空公开正文→空摘要，不回落隐藏正文）；再执行 Markdown 安全处理——复用 render_excerpt 纯文本提取（原始 HTML 事件剔除、链接目标剔除、标签结构不输出），公开可见；render_content（写路径）经 render_public_excerpt 生成摘要，隐藏正文只作为显式签名参数界定语义边界；commit=0a8a11e；review=单测 4 项（摘要只含公开正文/空公开正文与仅空白→空摘要不回退隐藏/原始 HTML 与标签结构不泄漏/超长公开正文截断加省略号）+ 既有 4 项摘要测试保持 + 集成测试追加断言（重渲染后摘要含公开正文、绝不包含受限内容）+ make check/test 全绿
- [x] `M04-MARKDOWN-07` `P0` `[45m]` 建立 XSS corpus：事件属性、javascript/data URL、SVG、MathML、畸形 HTML 和 Unicode 绕过。证据：files=backend/tests/markdown_xss.rs；commands=cargo test --test markdown_xss（2 通过/0 失败，28 个 XSS 用例）；cargo test --lib（307 通过/0 失败）；cargo fmt --check 通过；cargo clippy --lib --tests -- -D warnings 0 警告；make check 全绿 RC=0；make test 全绿 RC=0；contract=XSS corpus 28 例对每个 payload 跑完整管线（CommonMark 渲染→原始 HTML 事件剥离→ammonia allowlist 清洗）并用**结构扫描器**断言最终输出无可执行结构：元素级禁用 script/svg/math/iframe/style/embed/object/form/meta/link/base/template/noscript；属性级禁用 on* 事件处理器、style、命名空间属性（xlink:* 等）、非 http(s)/mailto 绝对 URL 的 href/src；辅助子串断言 javascript:/data: 不以任何形式出现；覆盖类别=事件属性（img onerror/svg onload/body onload/iframe javascript src）、javascript/data URL（md 链接/图片语法、大小写混合、HTML 实体、数字实体、制表符/换行/空字节、百分号编码、协议相对、相对 URL、alt 属性突破）、SVG/MathML、畸形 HTML（嵌套 script/未闭合标签/属性无空格/双尖括号）、Unicode 绕过（全宽 javascript scheme/全宽 onerror/混宽 script）；扫描器只识别真实标签结构——pulldown-cmark 会把非法 inline HTML 转义为纯文本（不可执行），转义文本不误报；commit=50ec005；review=28 例全部通过结构扫描（非法输入经渲染层转义为文本或属性值数据、危险 scheme 的 href/src 被氨水剥离，残余仅为惰性文本）+ 良性内容仍渲染（https 链接带 rel/target、图片、表格、mailto、强调）+ make check/test 全绿
- [x] `M04-MARKDOWN-08` `P0` `[30m]` 前端只允许 `SafeHtml` 使用 `{@html}`，通过静态检查阻止其他 sink。证据：files=frontend/src/lib/components/SafeHtml.svelte,frontend/src/lib/components/ui/{Icon.svelte,icons.ts},frontend/src/routes/posts/[id]/+page.svelte,scripts/check-html-sinks.rb,Makefile；commands=ruby scripts/check-html-sinks.rb（HTML sink OK）；cd frontend && npm run check（svelte-check 0 errors 0 warnings）；npm test（262 通过/0 失败，含 SafeHtml 2 项+Icon 2 项新增）；npm run build 通过；make check 全绿（check-frontend 含 html-sinks 静态检查）RC=0；make test 全绿 RC=0；contract=SafeHtml.svelte=全应用唯一 {@html} sink（只接收后端已清洗的 HTML 渲染产物，前端不做 DOM 解析/再清洗）；scripts/check-html-sinks.rb 静态检查：{@html} 仅允许于 SafeHtml.svelte（只扫 .svelte 模板，注释已剥离防误报）、生产源文件禁止 DOM HTML sink（innerHTML/outerHTML 赋值、insertAdjacentHTML、document.write、DOMParser/parseFromString、createContextualFragment、iframe srcdoc）、测试文件豁免（jsdom 夹具合法使用 innerHTML），接入 make check 的 check-frontend；+page.svelte 帖子/回复正文改经 SafeHtml 渲染（M04-POSTS 起内容源切后端 body_html）；Icon.svelte 重构弃用 {@html}——icons.ts 静态图标标记解析为结构化节点、以编译期已知 SVG 元素渲染（保持 SVG 命名空间；{@html} 在 <svg> 内经 innerHTML/range 注入的是 HTML 命名空间元素不渲染），并顺带修复 icons.ts 潜在转义缺陷（\\\" 三重转义导致 d 属性含字面反斜杠、图标实际不渲染）；commit=afbf704；review=SafeHtml 组件测试 2 项（渲染清洗 HTML 含锚点 id/空 HTML 无元素）+ Icon 测试 2 项（SVG 命名空间断言 path.namespaceURI、未知图标空渲染）+ 全量 262 前端测试 + svelte-check/build + make check/test 全绿
- [x] `M04-MARKDOWN-09` `P1` `[30m]` 测试纯文本、代码、链接、图片、长文和无 JavaScript 渲染一致性。证据：files=backend/tests/markdown_consistency.rs,frontend/src/lib/testing/ssr/post-content-nojs.test.ts；commands=cargo test --test markdown_consistency（8 通过/0 失败）；cd frontend && npx vitest run src/lib/testing/ssr/post-content-nojs.test.ts（4 通过/0 失败）；cargo fmt --check 通过；cargo clippy --lib --tests -- -D warnings 0 警告；make check 全绿 RC=0；make test 全绿 RC=0；contract=后端渲染一致性 8 项（完整管线 render_and_sanitize）：纯文本→段落/空行分隔多段、代码块→language-* 类+原内容保留+块内 HTML 转义为 &lt;script&gt; 不可执行、链接→https/mailto 保留+rel/target 注入+javascript scheme 剥离、图片→src/alt 保留、长文→输出有界+逐字节确定、无 JS 友好→输出无 script/事件属性/style= 纯静态 HTML 即可完整展示、摘要一致性（标题/正文/链接文字进摘要、目标 URL 与标签不进）、写路径 render_content 一致性（body_html/restricted_html/excerpt/版本与管线出口一致）；前端无 JS 一致性 4 项（svelte/server SSR 渲染 SafeHtml）：正文/标题 SSR 输出可见、代码块语言类与内容完整、链接 rel/target 与图片 src/alt 完整、长文 SSR 输出逐字节确定；commit=6098e3c；review=8+4 项全部通过 + make check/test 全绿（两端共同保证：后端产出无脚本静态 HTML，前端 SafeHtml SSR 注入，禁用 JS 内容完整一致）
- [x] `M04-MARKDOWN-10` `P1` `[30m]` 记录渲染策略变更的迁移、缓存失效和回滚方案。证据：files=docs/MARKDOWN.md,docs/DOCUMENT-STATUS.md,docs/SECURITY.md；commands=make check 全绿 RC=0；make test 全绿 RC=0；contract=docs/MARKDOWN.md 完整记录：版本模型（POLICY_VERSION=RENDERER_VERSION+'+'+SANITIZER_VERSION，renderer 与 sanitizer 独立递增、任一变更即判定存量行 stale）；升级流程（改代码+递增版本常量→全量门禁+XSS corpus→部署（不产生数据库迁移，禁止改已执行迁移）→enqueue_rerender_jobs 为每行入队 markdown.rerender Job（幂等 dedup）→worker 重渲染覆盖渲染产物（markdown 快照与元数据不变）→验证无 stale）；缓存失效（post_contents 投影直读无二级缓存/HTTP 缓存键须含 renderer_version/浏览器 no-cache+ETag/搜索索引由 search.index 独立维护不级联/前端 SSR+SafeHtml 无客户端正文缓存）；回滚方案（markdown 原文永不丢失，代码回滚+版本常量回旧值→行判定 stale→重渲染恢复旧产物，与升级对称、可不停机分批）；验证矩阵与参考实现清单；DOCUMENT-STATUS.md 登记该文档；SECURITY.md §6 交叉引用并修正外链 rel 值（nofollow noopener noreferrer）与 SafeHtml 命名；commit=79262c1；review=文档与实现一致（版本/Job/幂等/回滚语义逐一核对 rerender.rs/policy.rs/SafeHtml.svelte）+ make check/test 全绿

## M04-POSTS：草稿、文章与讨论服务

**元数据：** `P1` · `owner=backend-content` · `risk=high` · `depends=M04-MARKDOWN` · `blocked=none`
**目标文件：** `backend/src/content/posts/`、`backend/src/routes/{posts,drafts}/`、`backend/tests/posts/`
**验收：** Posts/Drafts/Revisions operation 在三数据库通过 CRUD、调度、权限和冲突契约。

- [x] `M04-POSTS-01` `P1` `[30m]` 定义 article/discussion 创建命令和服务端字段校验，不信任 author、状态或统计值。证据：files=backend/src/content/posts/{mod.rs,command.rs},backend/src/content/mod.rs；commands=cargo test --lib content::posts（12 通过/0 失败）；cargo test --lib（319 通过/0 失败）；cargo fmt --check 通过；cargo clippy --lib --tests -- -D warnings 0 警告；make check 全绿 RC=0；make test 全绿 RC=0；contract=创建命令（content/posts/command.rs）：CreatePostInput/CreateDraftInput=客户端原样内容字段（未信任），validate_post_create/validate_draft_create 产出服务端权威命令 CreatePostCommand/CreateDraftCommand；校验规则全部服务端权威（客户端放宽上限无效）：post_type∈{article,discussion}、title 1-200 字符（trim 后，PostTitle）、markdown 1-50000 字符（Unicode char，PostContent；隐含 markdown 格式）、board_id 必须合法 UUID（草稿可空，发布时定）、visibility_level ≥1 且不得超过作者当前等级（防低等级作者隐藏到更高级别；缺省 1）、access_policy 封闭枚举（策略明细校验随 POSTS-05）、scheduled_at 可选且必须严格晚于服务端当前时间、client_request_id 16-200 字符（幂等键）；不信任原则：命令结构只含 8 个内容字段，author_id/status/version/view_count/reply_count/pinned_at/featured_at 等服务端权威字段从类型层面不存在（Debug 断言核对）；PostCreateError 稳定 Display 且不回显原始输入；commit=d7db218；review=12 项单测（合法通过含类型/未知类型拒绝/discussion 接受/标题正文边界/UUID 非法/等级矩阵（缺省 1、等级内合法、超等级拒绝、<1 拒绝）/未知 policy/定时（未来通过、现在与过去拒绝）/幂等键长度/草稿可空板块+非法拒绝/无服务端权威字段/错误消息稳定）+ 全量门禁全绿
- [x] `M04-POSTS-02` `P1` `[45m]` 实现创建草稿、读取自己的草稿和 cursor 列表。证据：files=backend/src/routes/drafts.rs,backend/src/content/posts/{mod.rs,command.rs},backend/tests/drafts_routes.rs,todo/openapi-operation-coverage.json；commands=cargo test --test drafts_routes（11 通过/0 失败，含 POSTS-02 6 项）；cargo test --lib content::posts（15 通过/0 失败）；cargo test --lib（322 通过/0 失败）；cargo fmt --check 通过；cargo clippy --lib --tests -- -D warnings 0 警告；ruby scripts/check-route-coverage.rb（route-coverage OK）；ruby scripts/check-roadmap.rb（Roadmap OK）；make check 全绿 RC=0；make test 全绿 RC=0；contract=草稿创建 POST /api/v1/drafts：validate_draft_create 服务端权威校验（类型/标题/正文/板块 UUID（可空）/visibility_level≤作者等级/access_policy 枚举/定时未来/幂等键长度），author 取会话、board 可空、version 从 1 起，响应 201+Cashe-Control private,no-store；GET /api/v1/drafts/{id}：只读本人草稿（他人/不存在一律 404 不泄露存在性）；GET /api/v1/drafts：owner 维度 keyset 分页（updated_at DESC、after=上一页最后 updated_at、next_cursor 续页、limit 1-100）；路由注册（routes/mod.rs+app.rs merge）与 operation coverage（listDrafts/createDraft/getDraft→implemented+handler/tests/evidence）；commit=62f391f；review=路由集成 6 项（创建 201+private no-store+落库 owner/字段校验矩阵 400/未认证 401/未验证邮箱 403/读本人 200/他人与不存在 404/keyset 分页两页+next_cursor 空尾）+ 命令单测 15 项 + make check/test 全绿
- [x] `M04-POSTS-03` `P1` `[45m]` 实现草稿更新、client_request_id 幂等、版本冲突和软删除。证据：files=backend/src/routes/drafts.rs,backend/src/content/posts/command.rs,backend/tests/drafts_routes.rs,todo/openapi-operation-coverage.json；commands=cargo test --test drafts_routes（11 通过/0 失败，含 POSTS-03 5 项）；cargo test --lib content::posts（15 通过/0 失败，含 draft_patch 3 项）；cargo fmt --check 通过；cargo clippy --lib --tests -- -D warnings 0 警告；ruby scripts/check-route-coverage.rb OK；make check 全绿 RC=0；make test 全绿 RC=0；contract=草稿更新 PATCH /api/v1/drafts/{id}：If-Match 必须且与当前 version 一致（冲突→409 version_conflict），部分更新（validate_draft_patch 逐字段校验：标题/正文/板块 UUID/visibility_level≤作者等级/policy 枚举/定时未来且 null 清除），update_draft 递增 version，响应 200 Draft+private no-store；client_request_id 幂等（scope draft.create，POST 创建）：同 key+请求摘要重放返回原草稿（201）、不同摘要→409、进行中→409、失败缓存→409；软删除 DELETE /api/v1/drafts/{id}：deleted_at 置位行保留（审计/恢复）、删除后读 404、列表不含、可选 If-Match 防并发覆盖，响应 204；operation coverage updateDraft/deleteDraft→implemented；commit=62f391f；review=命令单测 3 项（部分更新/定时 null 清除/非法字段矩阵含超等级与过期定时）+ 路由集成 5 项（更新 200 version=2 且未提交字段保持/版本冲突 409+缺 If-Match 400/软删除 204+删除后 404+列表为空+行保留 deleted_at/同 key 重放返回同一 id 且仅一行/同 key 不同摘要 409）+ make check/test 全绿
- [x] `M04-POSTS-04` `P1` `[45m]` 实现预览，只返回当前用户临时安全 HTML，不写公开索引或缓存。证据：files=backend/src/routes/drafts.rs,backend/tests/drafts_routes.rs,openapi/openapi.yaml,scripts/{check-openapi,sync-operation-coverage,check-roadmap}.rb,todo/openapi-operation-coverage.json；commands=cargo test --test drafts_routes（14 通过/0 失败，含 preview 3 项）；cargo test --lib（322 通过/0 失败）；cargo fmt --check 通过；cargo clippy --lib --tests -- -D warnings 0 警告；ruby scripts/check-openapi.rb（OpenAPI OK 184 操作、Manifest 184/184）；ruby scripts/check-route-coverage.rb OK；ruby scripts/check-roadmap.rb（Roadmap OK、OpenAPI coverage 184/184）；make check 全绿 RC=0；make test 全绿 RC=0；cd frontend && npm test（266 通过/0 失败）；contract=POST /api/v1/drafts/{id}/preview：只渲染**当前用户自己的草稿**（他人/不存在→404 不泄露存在性），经 render_content 全管线（CommonMark→allowlist 清洗→公开摘要）产出临时安全 HTML 返回 {html, restricted_html, excerpt}；**不写任何状态**：不落 post_contents（公开索引）、不入搜索索引、不入队 Job、不写幂等记录、草稿行不被改写/版本不递增；响应 Cache-Control private,no-store；契约扩展：新增 previewDraft 操作（POST /drafts/{id}/preview，requestBody DraftPreviewRequest{markdown, restricted_markdown}，响应 DraftPreview{html, restricted_html, excerpt}，带 IdempotencyKey 参数）与 DraftPreviewRequest/DraftPreview schema，OpenAPI/覆盖清单基线 183→184（check-openapi/sync-operation-coverage/check-roadmap 三处脚本同步计数），TS types 重新生成；commit=a2f752c；review=路由集成 3 项（预览返回渲染+清洗 HTML 与摘要且 private no-store/预览不持久化：草稿正文与 version 不变、post_contents 0 行、jobs 0 入队/预览他人草稿 404）+ 既有 11 项保持 + make check/test 全绿
- [x] `M04-POSTS-05` `P0` `[45m]` 发布前重新读取作者等级、账号状态、板块规则、附件状态和 access policy。证据：files=backend/src/content/posts/publish.rs,backend/src/content/posts/mod.rs,backend/tests/posts_publish_preflight.rs；commands=cargo test --lib content::posts（19 通过/0 失败，含 publish 4 项）；cargo test --test posts_publish_preflight（6 通过/0 失败）；cargo test --lib（326 通过/0 失败）；cargo fmt --check 通过；cargo clippy --lib --tests -- -D warnings 0 警告；make check 全绿 RC=0；make test 全绿 RC=0（session_login 已知波动隔离通过/重跑通过）；contract=publish_preflight（P0）在发布写事务前**重新读取**全部服务端权威状态、绝不信任草稿/客户端缓存：①账号状态=authorize_action(post.create) 实时门（邮箱验证/24h 冷静期/mute/账号封禁等，DenyReason→稳定消息）；②作者当前等级=users.level 重读，visibility_level≤作者等级（防升级前缓存的高隐藏级别，超等级阻断 VisibilityExceedsLevel{requested,author_level}）；③板块规则=存在/未删除/is_active=1/posting_mode∈{normal,approval}（readonly/closed/停用/不存在阻断 BoardNotAcceptingPosts）；④附件状态=引用必须存在且属于作者（attachments 表 M6 落地；当前有非空引用且表不存在→明确拒绝而非静默忽略，表存在后校验自动生效）；⑤access policy 结构=level 需 min_level、paid 需 currency_id+amount 且为正（复用 ContentAccessPolicy::validate）；PublishBlocked 稳定 Display 不回显原始输入；commit=2aab4d3；review=单测 4 项（public 通过/level 需 min_level/paid 需 currency+正金额/未知 policy 拒绝）+ 集成 6 项（正常态通过/未验证与冷静期阻断/封禁阻断/超等级阻断且同级通过/板块矩阵（不存在、停用、只读、关闭、恢复 normal 通过）/附件表未落地拒绝）+ make check/test 全绿
- [x] `M04-POSTS-06` `P1` `[45m]` 实现即时发布和 scheduled 发布 Job；执行时再次运行全部授权和等级校验。证据：files=backend/src/content/posts/{service.rs,publish_job.rs,mod.rs},backend/src/routes/posts.rs,backend/tests/posts_publish.rs,docs/JOBS.md,todo/openapi-operation-coverage.json；commands=cargo test --test posts_publish（5 通过/0 失败）；cargo test --lib（326 通过/0 失败）；cargo fmt --check 通过；cargo clippy --lib --tests -- -D warnings 0 警告；ruby scripts/check-route-coverage.rb OK；make check 全绿 RC=0；make test 全绿 RC=0；contract=即时发布 createPost（替换骨架）：validate_post_create 字段校验→authorize_action(post.create)→publish_new_post（publish_preflight 再次授权与等级校验→事务写 posts/post_contents/post_revisions（初始 revision version=1，editor=author）→即时发布板块 post_count+1 + 搜索索引 Job 入队）；scheduled_at 为未来→posts.status='draft'（不计数、不入索引），content.publish Job（kind、payload {source:post,id}、dedup content:publish:post:{id}）由 enqueue_due_publish_jobs 扫描 posts WHERE status='draft' AND scheduled_at<=now 入队，handle_publish_job **执行时再次运行全部授权与等级校验**（publish_scheduled_post 内 publish_preflight）→事务切换 published+published_at+板块计数+搜索索引入队；已发布/不存在→幂等成功，预检阻断/DB 错误→瞬时重试，无效 payload→永久死信；commit=d216cc7；review=集成 5 项（即时发布 201+posts/post_contents/post_revisions 三表落库+渲染清洗断言+板块计数+搜索索引入队/未知板块 409 且零行/scheduled 全生命周期：创建 draft 态不计数不入队→未到期不入队→改到期入队 1→Job 执行 Succeeded→published+published_at+计数+索引+已发布重放幂等成功/执行时封禁作者→Failed 且保持 draft 不入索引/无效 payload 永久失败）+ 门禁全绿
- [x] `M04-POSTS-07` `P1` `[45m]` 实现详情、列表、板块列表和作者列表的 cursor/ETag/Cache-Control。证据：files=backend/src/routes/{posts.rs,boards.rs},backend/tests/posts_read.rs,openapi/openapi.yaml；commands=cargo test --test posts_read（4 通过/0 失败）；cargo test --lib（326 通过/0 失败）；cargo fmt --check 通过；cargo clippy --lib --tests -- -D warnings 0 警告；ruby scripts/check-route-coverage.rb OK；make check 全绿 RC=0；make test 全绿 RC=0；contract=GET /api/v1/posts（listPosts）：keyset cursor（created_at DESC, id DESC、after=上一页最后 created_at、fetch limit+1 判 has_more）、可选 board_id/author_id（作者列表投影）/sort（latest/popular），返回 PostPage{items, page{next_cursor, has_more}}；GET /api/v1/posts/{id}（getPost）：详情投影（post_type/author/status/slug/body_html/excerpt/access_summary{policy,unlocked}/capabilities）+ 浏览量自增（非关键路径失败忽略），published/hidden 可见、deleted 404；GET /api/v1/boards/{slug}/posts（listBoardPosts）：同 cursor 投影（created_at DESC）+ PostPage shape，板块不存在/已删除 404；所有只读响应 Cache-Control public,max-age=60 + ETag（body 摘要）；OpenAPI 增量：listPosts 增加 author_id query 参数；commit=ded28a9；review=集成 4 项（列表两页 keyset（最新在前/has_more/next_cursor/头）+ 详情投影（body_html/author/access_summary/Cache-Control/ETag/404）+ 板块过滤与 404 + 作者过滤只含该作者）+ 门禁全绿
- [x] `M04-POSTS-08` `P1` `[45m]` 实现编辑并创建不可变 revision；管理员代发/代改要求 reason、recent-auth 和审计。证据：files=backend/src/content/posts/service.rs,backend/src/routes/posts.rs,backend/tests/posts_edit.rs；commands=cargo test --test posts_edit（4 通过/0 失败）；cargo test --lib（326 通过/0 失败）；cargo fmt --check 通过；cargo clippy --lib --tests -- -D warnings 0 警告；make check 全绿 RC=0；make test 全绿 RC=0；contract=service::edit_post(EditPostInput{title,markdown,expected_version,change_reason})：If-Match 版本守卫（不一致→PublishError::VersionMismatch→409 version_conflict）→事务：post_contents upsert（markdown 提供则重新渲染 body_html/excerpt/renderer_version）+ 插入**不可变修订** post_revisions（version=旧+1，UNIQUE(post_id,version) 每版恰好一条；body_markdown 原文/editor/version/created_at 写入后不变，重渲染 Job 只覆盖 html/excerpt）+ posts title/version+1/updated_at；PATCH /api/v1/posts/{id}：作者本人→post.edit_own；非作者→**管理员代改**：post.moderate 权限（无→403）+ reason 必填（缺→400）+ recent-auth step-up（会话 auth_verified_at 超 5 分钟窗口→403 step_up_required）+ AuditEntry::delegated_admin_action(operator, effective_role, "post.update", "post", id, reason).record（必写审计日志）；commit=d07c301；review=集成 4 项（作者编辑 200 version=2+新 revision 快照为编辑后正文+post_contents 更新/版本冲突 409 code=version_conflict/无权限非作者 403 且作者本人仍可编辑/管理员代改全生命周期：缺 reason 400→过期会话 403 step_up_required→mark_step_up 后 200 version=2+audit_logs 一条 action=post.update 含 reason）+ make check/test 全绿
- [x] `M04-POSTS-09` `P1` `[30m]` 实现 pin/feature/close/move/merge 的 domain command 接口，具体治理权限接 M5。证据：files=backend/src/content/posts/govern.rs,backend/src/content/posts/mod.rs,backend/tests/posts_govern.rs；commands=cargo test --lib content::posts::govern（4 通过/0 失败）；cargo test --test posts_govern（3 通过/0 失败）；cargo test --lib（330 通过/0 失败）；cargo fmt --check 通过；cargo clippy --lib --tests -- -D warnings 0 警告；make check 全绿 RC=0；make test 全绿 RC=0；contract=治理命令接口（backend/src/content/posts/govern.rs）：PinCommand/FeatureCommand/CloseCommand/MoveCommand/MergeCommand 类型化命令 + validate() 服务端校验（post_id/target_board_id 必须 UUID、merge 源≠目标否则 MergeIntoSelf）；应用函数原子写：apply_pin/apply_feature/apply_close 置 pinned_at/featured_at/closed_at=now 或置空（帖子不存在→PostNotFound）、apply_move 目标板块须存在且 is_active=1（否则 TargetBoardNotActive）后更新 board_id、apply_merge 事务内评论迁移到目标帖+源帖软删(status=deleted/deleted_at)+目标帖 reply_count 累加源帖回复数；GovernError→AppError 映射供 M5 路由复用；具体治理权限（post.moderate/reason/审计）由 M5 接入，重定向/permalink 记录随 SEO 里程碑；commit=9aeba42；review=单测 4 项（pin UUID 校验/feature+close 校验/move 双 id 校验/merge 自身拒绝+合法通过）+ 集成 3 项（置顶/精选/关闭时间戳置位与清除、移帖到活跃板块成功+停用板块 TargetBoardNotActive、合并后评论迁移+源帖软删+reply_count 累加+自身合并校验拒绝）+ make check/test 全绿
- [ ] `M04-POSTS-10` `P0` `[45m]` 测试发布事务回滚、重复请求、定时执行时降级和旧 revision 并发覆盖。
- [ ] `M04-POSTS-11` `P1` `[30m]` 实现 revisions 列表/详情，普通作者只能看自身允许版本，管理查看写审计。
- [ ] `M04-POSTS-12` `P1` `[30m]` 更新 Posts/Drafts/Revisions operation coverage 证据。

## M04-COMMENTS：回复、引用与楼层

**元数据：** `P1` · `owner=unassigned/backend-content` · `risk=high` · `depends=M04-POSTS` · `blocked=none`
**目标文件：** `backend/src/content/comments/`、`backend/src/routes/comments/`、`backend/tests/comments/`
**验收：** Comments operation 的楼层、锁定、可见性、编辑和删除契约在三数据库一致。

- [ ] `M04-COMMENTS-01` `P1` `[45m]` 实现回复创建，重新检查主题、板块、actor 状态和回复开关。
- [ ] `M04-COMMENTS-02` `P1` `[30m]` 校验 parent/quote 属于同一主题且对 actor 可见，禁止跨主题引用泄漏。
- [ ] `M04-COMMENTS-03` `P0` `[45m]` 原子分配楼层号，在三数据库并发创建时唯一且连续语义明确。
- [ ] `M04-COMMENTS-04` `P1` `[45m]` 实现回复列表 cursor、稳定排序、parent 摘要和软删占位投影。
- [ ] `M04-COMMENTS-05` `P1` `[30m]` 实现作者限时编辑、版本冲突、revision 和清洗重渲染。
- [ ] `M04-COMMENTS-06` `P1` `[30m]` 实现作者删除与管理删除的不同状态、审计和通知行为。
- [ ] `M04-COMMENTS-07` `P0` `[45m]` 测试锁帖、禁言、隐藏主题、已删 parent、并发楼层和引用不可见内容。
- [ ] `M04-COMMENTS-08` `P1` `[30m]` 更新 Comments operation coverage 和三数据库证据。

## M04-VISIBILITY：内容访问策略与防泄漏

**元数据：** `P0` · `owner=unassigned/security-content` · `risk=critical` · `depends=M04-POSTS,M04-COMMENTS` · `blocked=none`
**目标文件：** `backend/src/content/visibility/`、`backend/tests/visibility/`、`docs/API-CONTRACTS.md`
**验收：** 隐藏标记字符串在 API/SSR/索引/Feed/通知/缓存/附件中均无法被未授权 persona 获取。

- [ ] `M04-VISIBILITY-01` `P0` `[30m]` 将 public/logged_in/after_reply/level/paid 定义为封闭枚举，拒绝指定用户可见。
- [ ] `M04-VISIBILITY-02` `P0` `[45m]` 实现统一 evaluate(actor, content, context) policy，返回 grant reason 而非直接返回正文。
- [ ] `M04-VISIBILITY-03` `P0` `[30m]` `visibility_level` 只允许 `1..作者当前等级`，越级稳定返回 `visibility_level_exceeds_author`。
- [ ] `M04-VISIBILITY-04` `P0` `[45m]` 创建、编辑、草稿发布、定时发布和管理员代发均重新检查当前作者等级。
- [ ] `M04-VISIBILITY-05` `P0` `[30m]` after_reply 只接受有效且可见的本人回复，删除/处罚后的 grant 语义按冻结规则实现。
- [ ] `M04-VISIBILITY-06` `P0` `[30m]` paid 只读取账本支持的有效 grant；扣款与 grant 创建由 M7 原子完成。
- [ ] `M04-VISIBILITY-07` `P0` `[45m]` 未授权 DTO 完全省略正文、摘要、搜索高亮、附件列表和可逆编码数据。
- [ ] `M04-VISIBILITY-08` `P0` `[30m]` 为公共/私有响应定义 Vary、ETag 和 Cache-Control，禁止跨 persona 304 泄漏。
- [ ] `M04-VISIBILITY-09` `P0` `[45m]` 建立一次可复用的投影过滤器供列表、详情、通知、Feed、SEO、AI 和附件调用。
- [ ] `M04-VISIBILITY-10` `P0` `[45m]` 用唯一 canary 字符串测试 API、错误、日志、审计 metadata、SSR 和 hydration 不泄漏。
- [ ] `M04-VISIBILITY-11` `P0` `[45m]` 测试等级边界、并发降级、after_reply、paid grant、封禁和管理员显式查看。
- [ ] `M04-VISIBILITY-12` `P1` `[30m]` 同步权限矩阵、错误码、OpenAPI 和 operation coverage。

## M04-UI：内容前端与端到端流程

**元数据：** `P1` · `owner=unassigned/frontend-content` · `risk=high` · `depends=M04-VISIBILITY` · `blocked=none`
**目标文件：** `frontend/src/routes/posts/`、`frontend/src/routes/editor/`、`frontend/src/lib/content/`、`frontend/tests/`
**验收：** article/discussion/draft/comment 的 SSR、键盘、移动端、冲突和隐藏正文 E2E 通过。

- [ ] `M04-UI-01` `P1` `[45m]` 实现文章、讨论列表和详情 SSR，使用后端安全投影而非浏览器再裁剪。
- [ ] `M04-UI-02` `P1` `[45m]` 实现编辑器 Markdown 输入/安全预览、字数状态和服务端字段错误。
- [ ] `M04-UI-03` `P1` `[45m]` 实现草稿保存、离开提示、恢复、删除和版本冲突 diff 提示。
- [ ] `M04-UI-04` `P1` `[30m]` 实现 article/discussion 切换、板块、标签、封面和定时发布时间表单。
- [ ] `M04-UI-05` `P0` `[30m]` 可见性选项只展示后端允许等级，前端篡改仍由 API 拒绝。
- [ ] `M04-UI-06` `P1` `[45m]` 实现回复、引用、编辑、删除、楼层定位和锁帖状态。
- [ ] `M04-UI-07` `P1` `[30m]` 实现 hidden/after_reply/level/paid 的可访问占位，不把正文放入 DOM。
- [ ] `M04-UI-08` `P1` `[30m]` 实现 409 版本冲突、422 等级变化、429 和审核中可恢复流程。
- [ ] `M04-UI-09` `P1` `[45m]` 测试无 JavaScript 公开阅读与发帖/回复表单的合理退化。
- [ ] `M04-UI-10` `P0` `[45m]` Playwright 断言未授权隐藏正文不出现在 HTML、hydration、网络缓存和浏览器状态。

---

<a id="m5"></a>

# M5：风险审核、举报、处罚、申诉与通知

**完成定义：** 普通内容默认发布；高风险内容进入人工审核；版主范围、利益冲突、处罚和申诉均服务端强制且可审计；通知不泄漏正文。

## M05-SCHEMA：治理与通知数据模型

**元数据：** `P0` · `owner=unassigned/backend-db` · `risk=high` · `depends=M04-VISIBILITY,M01-JOBS` · `blocked=none`
**目标文件：** `migrations/*/`、`backend/src/moderation/model*`、`backend/src/notifications/model*`
**验收：** 三数据库状态约束、去重、期限和不可变动作历史契约通过。

- [ ] `M05-SCHEMA-01` `P0` `[45m]` 新增 reports、moderation_cases、case_assignments 和内部 note 迁移。
- [ ] `M05-SCHEMA-02` `P0` `[45m]` 新增 moderation_actions/revisions，动作历史只追加不覆盖。
- [ ] `M05-SCHEMA-03` `P0` `[45m]` 新增 sanctions，覆盖 warning/rate_limit/mute/board_mute/ban、期限和撤销。
- [ ] `M05-SCHEMA-04` `P0` `[30m]` 新增 appeals、appeal decisions 和利益冲突 reviewer 约束所需字段。
- [ ] `M05-SCHEMA-05` `P1` `[45m]` 新增 notifications、notification_preferences 和 delivery dedup key。
- [ ] `M05-SCHEMA-06` `P0` `[30m]` 为举报 actor/target/reason 有效组合、重复窗口和状态建立索引/约束。
- [ ] `M05-SCHEMA-07` `P0` `[45m]` 测试非法状态、累计动作、到期、重复举报和不可变历史。
- [ ] `M05-SCHEMA-08` `P1` `[30m]` 同步 Schema、状态机、事件目录和保留策略。

## M05-RISK：发布前后风险评估

**元数据：** `P0` · `owner=unassigned/trust-safety` · `risk=critical` · `depends=M05-SCHEMA` · `blocked=none`
**目标文件：** `backend/src/moderation/risk/`、`backend/tests/moderation/risk*`、`docs/MODERATION.md`
**验收：** 低风险直接发布；高风险只进入人工队列；AI 失败/关闭不阻塞人工流程。

- [ ] `M05-RISK-01` `P0` `[30m]` 定义风险输入最小集合和版本化 policy，不向 Provider 暴露内部/隐藏数据。
- [ ] `M05-RISK-02` `P0` `[45m]` 实现新用户前 N 帖、链接数、重复内容、敏感词和频率规则。
- [ ] `M05-RISK-03` `P0` `[30m]` 普通内容保持先发布；命中高风险则原子设置 pending_review 且不进入公开投影。
- [ ] `M05-RISK-04` `P0` `[30m]` 定义 AI moderation suggestion 接口与禁用 Null Adapter，结果只能是建议。
- [ ] `M05-RISK-05` `P0` `[30m]` 禁止 AI 直接执行封禁、删除、放行、权限变更或账务动作。
- [ ] `M05-RISK-06` `P0` `[45m]` 作者状态投影只包含安全 reason category，不含举报人、内部 note、规则细节或 Prompt。
- [ ] `M05-RISK-07` `P0` `[45m]` 测试规则超时、AI 关闭/失败/迟到、重复评估和旧 policy 结果。
- [ ] `M05-RISK-08` `P1` `[30m]` 管理员可版本化更新风险阈值，要求 reason、审计和并发版本控制。
- [ ] `M05-RISK-09` `P1` `[30m]` 记录风险命中率、队列时长和误判反馈指标，不记录正文。

## M05-CASES：举报、案件与内容动作

**元数据：** `P0` · `owner=unassigned/backend-moderation` · `risk=critical` · `depends=M05-RISK,M03-AUTHZ` · `blocked=none`
**目标文件：** `backend/src/moderation/cases/`、`backend/src/routes/moderation/`、`backend/tests/moderation/`
**验收：** Moderation operation 的状态、范围、利益冲突、隐藏/恢复和审计测试通过。

- [ ] `M05-CASES-01` `P1` `[30m]` 实现帖子、回复、用户和附件举报 DTO、原因枚举与详情长度。
- [ ] `M05-CASES-02` `P0` `[30m]` 实现举报去重、撤回和统一响应，避免泄漏已有举报人与案件状态。
- [ ] `M05-CASES-03` `P0` `[45m]` 实现 open/triaged/investigating/resolved/rejected/reopened 状态机。
- [ ] `M05-CASES-04` `P0` `[30m]` 案件领取/转派验证版主板块范围、assignment 有效期和 actor 状态。
- [ ] `M05-CASES-05` `P0` `[30m]` 禁止处理自己、自己内容或明确利益冲突案件，并记录阻断审计。
- [ ] `M05-CASES-06` `P0` `[45m]` 实现 hide/restore/delete/move/lock/pin/feature/merge/edit_for_moderation command。
- [ ] `M05-CASES-07` `P0` `[30m]` 每个内容动作写 revision、reason、actor、policy version、审计和 Outbox。
- [ ] `M05-CASES-08` `P0` `[30m]` hide/delete 立即从详情、列表、搜索、Feed、SEO、缓存和附件投影撤除。
- [ ] `M05-CASES-09` `P0` `[45m]` restore/reopen 不复用旧公开缓存，重新运行当前风险、权限和可见性策略。
- [ ] `M05-CASES-10` `P0` `[45m]` 测试跨板块越权、自处理、并发处理、旧 version 和动作中途失败回滚。
- [ ] `M05-CASES-11` `P1` `[30m]` 更新 Moderation operation coverage 和动作审计证据。

## M05-SANCTIONS：处罚、实时生效与撤销

**元数据：** `P0` · `owner=unassigned/security-moderation` · `risk=critical` · `depends=M05-CASES,M02-SESSION` · `blocked=none`
**目标文件：** `backend/src/moderation/sanctions/`、`backend/tests/moderation/sanctions*`
**验收：** 处罚请求时实时生效；封禁撤销 Session/Refresh；到期和撤销不删除历史。

- [ ] `M05-SANCTIONS-01` `P0` `[30m]` 定义 warning/rate_limit/mute/board_mute/ban 的范围、叠加和优先级。
- [ ] `M05-SANCTIONS-02` `P0` `[45m]` 实现处罚创建，要求权限、板块范围、期限上限、reason 和近期认证。
- [ ] `M05-SANCTIONS-03` `P0` `[30m]` 请求时实时计算有效处罚，不把 worker 到期任务作为正确性边界。
- [ ] `M05-SANCTIONS-04` `P0` `[30m]` ban 同事务/提交后可靠撤销用户 Session，并投递 OIDC Refresh family 撤销事件。
- [ ] `M05-SANCTIONS-05` `P0` `[30m]` 实现处罚撤销/到期归档，只追加 reversal 记录，不改原处罚。
- [ ] `M05-SANCTIONS-06` `P0` `[45m]` 防止低权限版主处罚更高权限账号或超出板块/时长上限。
- [ ] `M05-SANCTIONS-07` `P0` `[45m]` 测试处罚即时生效、并发请求、到期边界、撤销、Session 和后续 OIDC 集成。
- [ ] `M05-SANCTIONS-08` `P1` `[30m]` 为用户提供安全处罚状态和到期时间，不泄漏内部依据或举报人。
- [ ] `M05-SANCTIONS-09` `P1` `[30m]` 增加处罚创建/到期/撤销安全通知和运营指标。

## M05-APPEALS：申诉与独立复核

**元数据：** `P1` · `owner=unassigned/backend-moderation` · `risk=high` · `depends=M05-SANCTIONS` · `blocked=none`
**目标文件：** `backend/src/moderation/appeals/`、`backend/tests/moderation/appeals*`
**验收：** submitted/reviewing/upheld/partially_upheld/rejected/withdrawn 合法迁移和权限测试通过。

- [ ] `M05-APPEALS-01` `P1` `[30m]` 实现可申诉对象、窗口、次数、文字长度和附件引用规则。
- [ ] `M05-APPEALS-02` `P1` `[45m]` 实现用户创建、列表、详情和未审理前撤回。
- [ ] `M05-APPEALS-03` `P0` `[30m]` 分配复核人时排除原处理者、自身、超范围和无有效 assignment 人员。
- [ ] `M05-APPEALS-04` `P0` `[45m]` 实现 uphold/partial/reject decision；接受申诉以补偿/撤销记录修正，不删历史。
- [ ] `M05-APPEALS-05` `P0` `[30m]` 申诉 DTO 分离用户说明与内部 note，双方投影不越界。
- [ ] `M05-APPEALS-06` `P1` `[45m]` 测试窗口、重复提交、利益冲突、并发 decision 和处罚撤销联动。
- [ ] `M05-APPEALS-07` `P1` `[30m]` 更新 appeals operation coverage、状态机和通知事件。

## M05-NOTIFY：站内通知与邮件投递

**元数据：** `P1` · `owner=unassigned/backend-notifications` · `risk=high` · `depends=M05-CASES,M01-JOBS` · `blocked=none`
**目标文件：** `backend/src/notifications/`、`backend/src/email/`、`backend/tests/notifications/`
**验收：** Notifications operation、Outbox/Job 重试、偏好和隐藏内容摘要测试通过。

- [ ] `M05-NOTIFY-01` `P1` `[30m]` 定义回复、引用、提及、审核、处罚、申诉、等级和安全通知模板键。
- [ ] `M05-NOTIFY-02` `P0` `[30m]` 通知 payload 只存资源 ID 与安全模板参数，不复制隐藏正文或内部 note。
- [ ] `M05-NOTIFY-03` `P1` `[45m]` 实现站内通知 cursor 列表、单条/批量已读和未读计数。
- [ ] `M05-NOTIFY-04` `P1` `[30m]` 实现类别偏好；强制安全通知不能被普通偏好完全关闭。
- [ ] `M05-NOTIFY-05` `P1` `[30m]` 以事件/收件人/template 建立去重，重放 Outbox 不重复通知。
- [ ] `M05-NOTIFY-06` `P0` `[45m]` 读取通知时重新检查目标权限；失权后只显示安全失效状态。
- [ ] `M05-NOTIFY-07` `P1` `[45m]` 邮件通过 Job 投递，处理临时/永久失败、退避、dead 和管理员重放。
- [ ] `M05-NOTIFY-08` `P0` `[30m]` 验证邮箱 token、完整邮箱、隐藏正文和 Provider 响应不进入日志。
- [ ] `M05-NOTIFY-09` `P1` `[45m]` 测试偏好、去重、资源隐藏/删除、邮件失败和无正文泄漏。

## M05-UI：举报、审核、申诉与通知前端

**元数据：** `P1` · `owner=unassigned/frontend-moderation` · `risk=high` · `depends=M05-APPEALS,M05-NOTIFY` · `blocked=none`
**目标文件：** `frontend/src/routes/moderation/`、`frontend/src/routes/admin/moderation/`、`frontend/src/routes/notifications/`、`frontend/tests/`
**验收：** member/board moderator/global moderator/admin persona 的 Playwright、键盘和泄漏测试通过。

- [ ] `M05-UI-01` `P1` `[30m]` 实现举报对话框/页面、原因、详情、成功统一状态和撤回入口。
- [ ] `M05-UI-02` `P1` `[45m]` 实现作者审核中/通过/拒绝状态，不展示风险细节或举报人。
- [ ] `M05-UI-03` `P1` `[45m]` 实现版主案件队列、筛选、详情、领取和范围提示。
- [ ] `M05-UI-04` `P1` `[45m]` 实现内容动作和处罚表单，要求 reason、期限、确认和 recent-auth 状态。
- [ ] `M05-UI-05` `P0` `[30m]` 自身案件、跨板块和高权限目标即使前端篡改也由 API 拒绝并呈现稳定错误。
- [ ] `M05-UI-06` `P1` `[45m]` 实现申诉创建、列表、详情、撤回和复核决策页面。
- [ ] `M05-UI-07` `P1` `[30m]` 实现通知列表、未读、偏好和失效资源安全状态。
- [ ] `M05-UI-08` `P0` `[45m]` Playwright 验证隐藏正文、内部 note、举报人和超范围案件不进入 DOM/hydration。
- [ ] `M05-UI-09` `P1` `[45m]` 测试键盘、焦点、移动端、减少动效和无 JavaScript 举报表单退化。
- [ ] `M05-UI-10` `P1` `[30m]` 完成 Moderation/Notifications operation coverage 和路由矩阵证据。

---

## M3-M5 出口门槛

- Users/Roles/Boards/Tags/Posts/Drafts/Revisions/Comments/Moderation/Notifications 对应 operation coverage 均有实现与测试证据。
- 所有 persona 的权限由 API 直接测试，版主板块范围和对象 owner 条件无遗漏。
- 隐藏正文 canary 不出现在 API、SSR、DOM、hydration、搜索、Feed、通知、日志、审计、附件或公共缓存。
- 创建、编辑、定时发布时都不能设置高于作者当前等级的可见等级。
- 高风险内容只进入人工队列，AI 不能直接处罚、删除或放行。
- 举报、处罚和申诉历史只追加；封禁实时撤销 Session，并为 OIDC 撤销留出可靠事件。
